## Overview
The add bar's live suggestion stack (both while typing and the empty-field "your usuals" list) currently shows up to 4 rows at once; the user wants it capped at 3.

## Do
In `Sources/Services/Suggestions.swift`, change the constant at line 16 from `static let combinedMax = 4` to `static let combinedMax = 3`. This single constant caps BOTH `Suggestions.combined(...)` (the live-typing prefix/dictionary + personal-history suggestion list, via `.prefix(combinedMax)` around line 56) and `Suggestions.usuals(...)` (the empty-query "your usuals" list, via `.prefix(combinedMax)` around line 84) — changing it affects both suggestion surfaces, which is the intended scope (they share the same visual add-bar stack in `AddBar.swift`, just for different query states). Do not touch `AddBar.swift` or `ShoppingListView.swift` — they consume whatever `Suggestions` returns and need no changes. Do not hand-edit any generated file (`EmojiTable.swift`, `SuggestionDictionary.swift`) — this task does not touch generators. Two existing unit tests in `Tests/BasketTests.swift` (`testCapsAtCombinedMax` around line 157-160, `testUsualsCapsAtCombinedMax` around line 195-198) assert `.count == Suggestions.combinedMax` symbolically (not a literal 4), so they should continue to pass unmodified once the constant changes — do not hardcode a literal 3 into these tests; if you find any OTHER test or UITest that hardcodes a literal count of 4 for suggestions (checked this session: none found in `Tests/SnapshotTests.swift`, `UITests/SuggestionsFlowTests.swift`, `UITests/UsualsFlowTests.swift`), update it to 3 or to reference `Suggestions.combinedMax` instead of a literal.

## Done when
`Sources/Services/Suggestions.swift`'s `combinedMax` constant is `3`. The full test suite (`xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`) passes, including `testCapsAtCombinedMax` and `testUsualsCapsAtCombinedMax` in `Tests/BasketTests.swift` (unmodified, still asserting against the symbolic `Suggestions.combinedMax`). `./build_run.sh` reaches `** BUILD SUCCEEDED **`. Manually verify in the simulator (per this repo's visual-verification convention) that typing a query with 4+ matching history/dictionary items shows at most 3 suggestion rows above the add bar, and that focusing an empty add bar with 4+ "usual" items also shows at most 3 rows. README.md updated if it documents the suggestion count anywhere.
