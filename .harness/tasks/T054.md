## Overview
The 'Clear all' soft-delete/undo flow is a second state machine living raw inside Sources/Views/ShoppingListView.swift: @State pendingClearIDs/showClearToast/clearedToastCount/clearToken (~lines 29-34) plus clearGot()/commitClear()/undoClear() (~lines 383-425), where a token-invalidated DispatchQueue.main.asyncAfter performs the app's ONLY destructive operation (permanent SwiftData deletes). The repo already proved the right pattern on the other in-view state machine: CheckOffChoreography is a pure, documented, unit-tested value type in Sources/Services/ListLogic.swift (~lines 50-79). Extract a ClearChoreography value type in the same style, unit-test its races, and leave the view holding only timer scheduling, animation wrapping, haptics, and the context.delete calls. IMPORTANT: this task depends on T038, which changes commitClear() so the buffer is NARROWED to ids still present in the items query after the deletes (instead of removeAll()) to prevent a one-frame Got-it flicker, and shortens the toast duration. T038 will already be merged when this task runs - read the CURRENT code, not the line numbers above (they are approximate pre-T038 references), and the extraction MUST preserve T038's narrowed-buffer expiry semantics exactly.

## Do
1) Read the current clear-all code in Sources/Views/ShoppingListView.swift (state block near the top; clearGot/commitClear/undoClear near the bottom) and CheckOffChoreography in Sources/Services/ListLogic.swift as the style template (pure struct, generic over ID: Hashable, private(set) state, doc comments explaining the product behaviour). Note T038's semantics in commitClear(): after issuing context.delete for the buffered ids it does NOT empty the buffer; it narrows it to ids still present in `items`, so the frame where SwiftData's @Query hasn't refreshed yet keeps hiding the deleted rows from recentlyGot.
2) Add a `ClearChoreography<ID: Hashable>` value type to Sources/Services/ListLogic.swift (pure Swift, Foundation only - it must compile in the native harness). Suggested shape (the exact API is yours, but every behaviour below must be expressible and pure-testable): a private(set) hidden-buffer Set<ID> plus a monotonically increasing token; `isHidden(_ id:) -> Bool` for the recentlyGot filter; `mutating beginClear(_ ids: some Collection<ID>) -> (token: Int, count: Int)?` that unions into the buffer, bumps the token, and returns nil (no-op) when there is nothing new to clear; `mutating undo() -> Bool` that empties the buffer and bumps the token (invalidating any in-flight expiry), returning false when the buffer was already empty; `mutating expire(token: Int) -> Set<ID>?` that returns the ids to actually delete ONLY when the passed token is still current and the buffer is non-empty, else nil (stale-token / already-undone no-op) - and, critically, does NOT empty the buffer itself; `mutating narrow(stillPresent: (ID) -> Bool)` that drops buffered ids the predicate rejects - this is how T038's kept-hiding-until-the-query-catches-up semantics is preserved (the view calls it with `ids-still-in-items` right after issuing the deletes, and harmlessly again on later recomputes if you choose).
3) Rewire ShoppingListView to hold a single `@State private var clearChoreo = ClearChoreography<PersistentIdentifier>()` in place of pendingClearIDs + clearToken (showClearToast/clearedToastCount may stay as view state or be derived - your call). The view keeps ONLY: the DispatchQueue.main.asyncAfter(deadline: .now() + TestHooks.clearToastDuration) scheduling, the context.delete(item) loop, the withAppAnimation wrapping (reduceMotion variants unchanged), Haptics calls, and toast show/hide. The recentlyGot computed property filters via clearChoreo.isHidden instead of pendingClearIDs.contains. Behaviour must be IDENTICAL to before, including T038's no-flicker expiry.
4) House rules: do not introduce any bare Date()/Date.now (all current-time reads go through AppClock.now - this task should not need any) and any animation goes through withAppAnimation/.unlessUITesting, never bare withAnimation (TestHooks determinism switchboard).
5) Extend Tests/ListLogicTests.swift in its existing style (plain XCTest funcs, Int ids, one behaviour per test with a message where non-obvious) covering AT MINIMUM: (a) expire-after-undo is a no-op - beginClear, capture token, undo(), then expire(oldToken) returns nil and nothing is hidden; (b) double-clear supersedes a pending expiry - beginClear batch 1, beginClear batch 2, expire with batch 1's token returns nil (stale), expire with batch 2's token returns the FULL current buffer (both batches, since batch 1 was never deleted); (c) undo-after-expiry is a no-op - after expire's ids are 'deleted' and narrow(stillPresent:) drops them, undo() returns false and un-hides nothing; (d) a stale token can never return ids to delete; (e) narrowing preserves T038 semantics - after expire(token) returns the delete set, ids remain hidden (isHidden true) until narrow(stillPresent:) with a predicate excluding them, and narrow keeps ids the predicate still accepts.
6) Docs in the same commit: CLAUDE.md's project map and README's ListLogic/tests descriptions mention 'section partitioning + CheckOffChoreography' - update those one-liners to also name the clear/undo choreography. If the extraction bakes in any behavioural trade-off, note it in README.md (the owner records the .harness LIMITATIONS.md row separately; do NOT edit .harness/ files in this task); a faithful extraction should not need one.

## Done when
ClearChoreography exists in Sources/Services/ListLogic.swift as a pure value type in the CheckOffChoreography style (no SwiftUI/SwiftData imports needed by it). ShoppingListView no longer owns the raw state machine: `grep -n 'clearToken' Sources/Views/ShoppingListView.swift` finds nothing, and pendingClearIDs as a raw Set @State is gone (replaced by the choreography); the view retains only scheduling, deletes, animation, haptics, and toast presentation. commitClear-equivalent code still never unconditionally empties the buffer at expiry (T038's narrowed-buffer semantics preserved - confirm by reading the code). New ListLogicTests cover the five behaviours listed in the Do section and pass. No new bare Date()/Date.now or bare withAnimation was introduced. Docs updated in the same commit. `./build_run.sh` reaches ** BUILD SUCCEEDED **, and the full suite is green with the existing clear-all/undo UI tests UNMODIFIED (behaviour is unchanged, so they must pass as-is): `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`.
