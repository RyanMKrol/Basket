## Overview
The 'Got it' section's check control (CheckCircle in Sources/Views/ItemRow.swift) currently shows a filled green circle with a white checkmark once an item is checked off. Swap the glyph to an up-arrow so the same existing tap-to-untoggle affordance visually communicates 'tap to undo' instead of looking like a static, final checkmark.

## Do
In Sources/Views/ItemRow.swift's CheckCircle struct (around lines 212-238), change the `Image(systemName: "checkmark")` (line 229) shown when `isChecked` is true to `Image(systemName: "arrow.up.left")` — reusing the exact glyph already used for AddBar's live-suggestion row (Sources/Views/AddBar.swift line 32) for visual consistency with the app's existing 'undo-ish' iconography. Keep everything else about CheckCircle unchanged: the circle still fills with Theme.leaf green when isChecked, the same scale/opacity spring animation applies, and the tap-to-untoggle wiring (ItemRow.swift lines 101-110: onTapGesture calling onToggle, and the row-level/accessibility-action paths at lines 96-97 and 116 for got-it rows) is untouched — this is a glyph-only visual change, not a behavior change. Do NOT change the accessibility label ("Got it" / "Not got yet" at line 107) — it announces the item's checked STATE, not the tap action, so it stays as-is unless the owner's answer to the linked question says otherwise.

## Done when
Sources/Views/ItemRow.swift's CheckCircle renders "arrow.up.left" (not "checkmark") as its glyph when isChecked is true; the circle fill color, animation, and all existing tap/accessibility wiring for the Got-it row are unchanged (verify by reading the diff against ItemRow.swift lines 96-116 and 212-238 — those tap/accessibility lines should have zero changes). Tests/SnapshotTests.swift's testItemRowCheckedInGotSection (asserts an ItemRow with isChecked: true) has its reference image in Tests/__Snapshots__/ re-recorded to reflect the new arrow glyph — the snapshot test must pass against the new reference, not the old checkmark image. ./build_run.sh reaches BUILD SUCCEEDED and the full test command (xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro') passes, including AccessibilityAuditTests (unaffected, since the accessibility label doesn't change) and CheckOffFlowTests (which query by accessibilityIdentifier/label, not the glyph itself, so they should be unaffected by this swap).
