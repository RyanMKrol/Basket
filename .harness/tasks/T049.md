## Overview
Replace the hand-rolled native logic harness (tools/main.swift + a hand-listed swiftc invocation in CI) with a real test target inside the BasketCore package, run via `swift test` natively (no simulator, sub-second). Every existing check must be ported 1:1 BEFORE the old harness is deleted — no coverage may be lost in the swap.

## Do
## Do

1. Add a `BasketCoreTests` test target to `BasketCore/Package.swift` (sources in `BasketCore/Tests/BasketCoreTests/`) using Swift Testing (`import Testing`).
2. Port EVERY `check(...)` in tools/main.swift 1:1 into the new target — the file has ~150 assertions in these groups: emoji categories, T008 corrections, non-food, tricky prefix collisions, T009 short-keyword exact-word matching, T010 head-noun preference, history-ranking suggestions, semantic NLEmbedding fallback, combined history+dictionary suggestions, real SuggestionDictionary contents/capitalisation invariant, capitalisedFirstLetter, the full Measure block (typeForName classification, defaultUnit, step snapping, changeUnit, units(for:), format, numberString, parse), and Seasonality (timeOfDay, holidayAccent, emptyStateLine stability/rotation). Preserve each check's exact inputs, expected values, and fixed dates (e.g. `Date(timeIntervalSince1970: 1_700_000_000)`, the UTC-calendar season dates).
3. Express the table-style groups as parameterized `@Test(arguments:)` over (input, expected) tuples — at minimum the emoji keyword groups and the Measure typeForName/step/parse groups — so each case is an individually-reported test. Keep one suite per logical group.
4. Only after `swift test --package-path BasketCore` is green with the full port: delete `tools/main.swift`.
5. `.github/workflows/ci.yml`: replace the "Native logic harness" swiftc step with a `swift test --package-path BasketCore` step (keep it in the push-triggered `test` job, after or parallel to the simulator step; update the step name/comment).
6. Docs in the same commit: README.md Tests section (the tools/main.swift swiftc block becomes the `swift test` command) and CI description; CLAUDE.md "Definition of done" native-harness bullet, project map (`tools/` entry, `TipJar.swift is kept out of the tools/main.swift compile list` note), and any other `tools/main.swift` mention.
7. Do NOT touch `.harness/**` (LOCAL_DOD already runs `swift test --package-path BasketCore` conditionally, courtesy of the earlier owner task) and do NOT modify or delete the Tests/ XCTest suites — they keep running on the simulator.

## Done when
## Done when

- `swift test --package-path BasketCore` passes natively with every tools/main.swift check represented (spot-auditable: each print-group in the old file maps to a named suite, and the parameterized suites enumerate the same input/expected pairs).
- Table-style groups use `@Test(arguments:)` with per-case reporting.
- `tools/main.swift` is deleted; no reference to it or `basket_check` remains in ci.yml, README.md, or CLAUDE.md.
- ci.yml's push job runs `swift test --package-path BasketCore` and CI is green.
- The full simulator suite (`xcodebuild test -scheme Basket`) is still green and Tests/ is untouched.
