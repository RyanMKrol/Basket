## Do
In Sources/Views/ShoppingListView.swift, the private computed property `titleView` (around lines 161-184) renders the app title next to the tipped-supporter heart. Its HStack uses `alignment: .firstTextBaseline` (line 162), containing either `rainbowTitle` or `Text("Basket")` set at `Theme.title(34, weight: .bold)` (lines 164/166), and, when `supporter` is true, an `Image(systemName: "heart.fill").font(.system(size: 17)).foregroundStyle(.red)` (lines 168-172). SF Symbol images don't share the same baseline metrics as text glyphs, so at a large 34pt title size the heart currently reads as vertically off (too low, sitting below the visual center of the letters) relative to the title. Fix the heart's vertical position so it looks optically centered against the title text -- e.g. via a `.alignmentGuide` on the Image to nudge it relative to `.firstTextBaseline` (or `.center`), or swapping the HStack's alignment mode plus a small compensating offset. Do not change the title font, the heart's size/color, the rainbow-toggle behavior, or the tap gesture/accessibility labels already on titleView.

## Done when
Building and running the app (tipJar.hasTipped == true) shows the red heart glyph optically centered against the "Basket" title's cap-height in both the rainbow and classic title states, with no other visual or behavioral change to the header (title font/size, heart size/color, tap-to-toggle, accessibility labels all unchanged).
