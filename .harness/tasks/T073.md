## Overview
Re-attempts T069, which was ITSELF a re-attempt of T043. T043 blocked on a scope gate (its spec told the builder to edit .harness/custom/docs/LIMITATIONS.md, outside scope); T069 fixed THAT by re-scoping to tools/README.md/.gitignore. But T069 STILL blocked — and the new root cause is NOT a spec/scope bug and is NOT fixable by any re-spec. It is ENVIRONMENTAL and fundamental: xcprof's CPU profiling works by attaching xctrace to a running process, and on this build machine `xctrace --attach` never sees the Simulator-hosted Basket process while it is alive (only after it exits, producing an empty trace), while `--device` misroutes the CPU Profiler tap setup to a paired physical iPhone via CoreDeviceService and hangs indefinitely. The consequence: an unattended cold builder CANNOT produce a real, non-empty `tools/perf-baseline/baseline.json` in one pass — T069's DoD required exactly that (a baseline with Basket-attributed hot frames, plus a live compare-exit-3 demonstration), so the task was un-buildable-as-specified regardless of model tier. It escalated haiku->sonnet and sonnet correctly reported failed:blocked rather than fabricating a trace. NOTHING landed (no tools/profile_app.sh, no tools/perf-baseline/, no README perf section). This unit carves out the part that IS automatable in a cold pass: the profiling script itself plus the documented compare workflow, with the baseline-capture and live-compare demonstration REMOVED (handed to the paired needs-human unit review-t069-b). The verify contract drops the `jq -e tools/perf-baseline/baseline.json` check that no automatable pass can satisfy.

## Do
Tooling + developer-docs only. Touch ONLY tools/profile_app.sh, README.md, and .gitignore. Do NOT edit any .harness/ file, app code, project.yml, Tests/, UITests/, or ci.yml. Do NOT change any TASKS.json status. Do NOT attempt to record a real trace or commit a baseline.json (that is the paired needs-human unit's job) — this unit must build cleanly WITHOUT a working attach.

1. Create `tools/profile_app.sh` (executable, `set -euo pipefail`), modeled on the existing `tools/record_ui_test.sh` (read it plus `build_run.sh` first for repo-root resolution, the Basket-Claude simulator-name->UDID resolution via `tools/loop_sim.sh`, and the background-process + `trap cleanup EXIT` pattern). Usage: `tools/profile_app.sh [test-identifier] [simulator-name] [output-label]`.
   - `--help`/`-h` MUST print usage and exit 0 BEFORE any xcprof lookup (this is the only automatable verify, so it must work on a machine without the Axiom plugin).
   - Default workload: `BasketUITests/SuggestionsFlowTests/testTappingSuggestionChipAddsItem`; the test-identifier arg overrides it. Default simulator: `Basket-Claude` (via loop_sim.sh), NEVER the shared `iPhone 17 Pro`.
   - Locate xcprof with graceful degradation: `command -v xcprof`, else glob `$HOME/.claude/plugins/cache/axiom-marketplace/axiom/*/bin/xcprof` and pick the highest version (`sort -V | tail -1`). If none found, print an actionable message naming that plugin-cache path and exit 2.
   - Run `xcprof doctor` once; if xctrace is unavailable, exit 2 with a clear message.
   - Record around the workload: `xcodegen generate`, then start `xcodebuild test ... -only-testing:"$TEST_ID"` on Basket-Claude in the background; poll (bounded ~60s) for the host-visible Basket process (`pgrep -x Basket`); then `"$XCPROF" record --preset cpu --attach Basket --no-prompt --time-limit 30s --output "$TRACE_PATH"`. Wait on xcodebuild and fail if the test failed.
   - CRITICAL — attach reliability guard: because `--attach` is known to silently miss the Simulator process on this class of machine (see the README caveat below), after `analyze` the script MUST detect an empty/Basket-frameless trace and exit NON-ZERO with a message pointing the user at the README 'why attach may see nothing' note and the needs-human baseline step. Never let an empty trace masquerade as success.
   - Traces under `build/perf-traces/<label>.trace` (covered by the existing `build/` .gitignore entry — never commit .trace bundles; confirm .gitignore already ignores build/, add an explicit `build/perf-traces/` line only if not already covered). Label defaults to a timestamp; positional output-label overrides.
   - Analyze: `"$XCPROF" analyze "$TRACE_PATH" --json` -> `build/perf-traces/<label>.json`; print the human markdown to stdout; echo the exact `xcprof compare` command to run next.
   - A `--refresh-baseline` flag copies the analyze JSON to `tools/perf-baseline/baseline.json` (pretty-printed via `jq .`), creating `tools/perf-baseline/` if absent — but this path only succeeds when a non-empty trace was captured (i.e. it will NOT succeed unattended on this machine; that is expected and is the needs-human unit's territory).
2. README.md — add a 'Performance profiling (CPU)' section under the tools/testing material covering: what `tools/profile_app.sh` does and its args; the fixed default workload and why it must stay fixed; that `xcprof compare` needs two .trace bundles from the SAME machine and `tools/perf-baseline/baseline.json` (once a human records it) is a reference snapshot only; the local A/B recipe (record baseline on main -> record current on branch -> `xcprof compare A B --fail-on-regression`, exit 3 = regression); the VERBATIM honesty caveat that this is CPU-only — memory/Leaks instruments are not exportable headlessly, so this is NOT leak detection; that CI gating is deliberately deferred (GitHub runners lack the plugin, cross-machine baselines are noise); AND a new, explicit 'why attach may see nothing on this machine' subsection stating that `xctrace --attach` does not observe the Simulator-hosted Basket process while alive on this Mac (and `--device` misroutes to a paired iPhone and hangs), so recording a real baseline is a HUMAN step done on suitable hardware — pointing at the paired needs-human task. Plain tone, no em dashes.

Do NOT add a LIMITATIONS.md row or touch any .harness/ file. Do not add per-task model/effort fields anywhere.

## Done when
- `bash -n tools/profile_app.sh` passes and the script is executable.
- `tools/profile_app.sh --help` prints usage and exits 0 on a machine WITHOUT xcprof (help resolved before tool discovery); with xcprof absent a normal run exits 2 with an actionable message naming the Axiom plugin-cache path.
- The script detects an empty / Basket-frameless trace and exits NON-ZERO rather than writing a bogus baseline (the attach-reliability guard).
- README.md has the 'Performance profiling (CPU)' section including the verbatim CPU-only honesty caveat (memory/Leaks not exportable headlessly — not leak detection), the explicit statement that CI gating is deferred, and the new 'why attach may see nothing on this machine' subsection deferring live baseline recording to the paired human task.
- No `.trace` bundle is committed; `tools/perf-baseline/baseline.json` is NOT required by this unit (it is the needs-human unit's deliverable).
- The diff touches ONLY tools/profile_app.sh, README.md, and .gitignore — no .harness/ file, no app code, no project.yml, no Tests/UITests/, no ci.yml.
