## Overview
Three tiny, independent, behaviour-preserving cleanups surfaced by Axiom audits. (1) AnyView type erasure: ShoppingListView.editor(for:) (~lines 293-307) returns AnyView?, threaded through ToGetSection's `editor: (GroceryItem) -> AnyView?` (ToGetSection.swift:13) into ItemRow's `var quantityEditor: AnyView? = nil` (ItemRow.swift:31, rendered at ~118-119) - AnyView defeats SwiftUI's structural identity/diffing; the fix is mechanical. (2) Dead GeometryReader: Sources/Theme/Theme.swift (~lines 103-108) BasketBackground wraps the dot backdrop in a GeometryReader capturing geo.size into w/h, which dots(_:_:_:) receives and NEVER uses - the Canvas inside gets its own `size`; pure deletion. (3) Sources/Services/Haptics.swift defines prepare() (lines 10-15) pre-warming the three generators, but a repo-wide grep confirms it is never called anywhere - either dead code to delete or a missing pre-warm; the owner decides via the pending question. HARD BOUNDARY: pending task T038 owns ShoppingListView's clear-all code - do NOT touch clearGot()/commitClear()/undoClear(), pendingClearIDs/showClearToast/clearedToastCount/clearToken, or TestHooks.clearToastDuration. This task's only ShoppingListView change is the editor(for:) signature/plumbing.

## Do
1) AnyView removal: eliminate AnyView from the quantity-editor path. Two mechanical options - pick one: (a) make ItemRow generic over its editor (`struct ItemRow<Editor: View>` with `var quantityEditor: Editor?`), adding an EmptyView-constrained convenience (e.g. an extension init or default) so the editor-less call sites - GotItSection.swift:12 and the three SnapshotTests.swift constructions (lines 53/59/65) - compile unchanged or with a minimal, behaviour-free tweak, and thread the generic through ToGetSection (its `editor` closure and ShoppingListView.editor(for:) return the concrete QuantityEditor? instead of AnyView?); or (b) construct the editor inside ToGetSection so the closure passes data, not erased views. Whichever you pick, ShoppingListView.editor(for:) must no longer mention AnyView, and the guard logic inside it (expandedID match + unit/quantity presence) must be preserved wherever it lands.
2) Dead GeometryReader: in Sources/Theme/Theme.swift's BasketBackground, delete the GeometryReader wrapper and the unused w/h parameters from dots(...) - the Canvas already sizes itself. Keep the ZStack ordering (paper, dots, timeTint) and .ignoresSafeArea() exactly as-is. Purely structural; the rendered pixels must not change.
3) Haptics.prepare(): OWNER DECISION - a pending question asks whether to delete it as dead code or wire the pre-warm. Apply whichever answer the consolidation recorded alongside this spec; if no answer is recorded, DEFAULT to deleting prepare() (smallest change) and update the type's doc comment (which currently says 'via prepared generators') so it stays truthful. If the owner instead chose wiring: call Haptics.prepare() once from ShoppingListView's .onAppear (already in scope) so the first check-off haptic hits warm generators - do not add new files or new call sites elsewhere.
4) House rules: no new Date()/Date.now (AppClock.now only, though this task should need none) and no bare withAnimation/.animation - use withAppAnimation/.unlessUITesting if any animation code moves. Do not touch the clear-all code or TestHooks (see boundary above). All three items are behaviour-preserving: snapshot reference images in Tests/__Snapshots__/ must NOT be re-recorded.
5) Docs: no README changes expected (internal plumbing); if you find a genuine trade-off (e.g. a generic ItemRow constraining a future caller), record it in README.md (the owner records the .harness LIMITATIONS.md row separately; do NOT edit .harness/ files in this task).

## Done when
`grep -rn 'AnyView' Sources/` returns no hits (the only current uses are the editor plumbing). `grep -n 'GeometryReader' Sources/Theme/Theme.swift` returns no hits, and dots() no longer takes width/height parameters. The Haptics.prepare() decision is applied exactly one way: either the function is gone and `grep -rn 'prepare' Sources/` shows only the UIKit generator calls removed with it, or it is called exactly once (ShoppingListView .onAppear). ShoppingListView's clearGot/commitClear/undoClear and the clear-all @State block are byte-identical to before this task (T038 owns them). `git status` shows no modified or new files under Tests/__Snapshots__/ (references unmodified), and the snapshot tests PASS against those unmodified references - that is the proof all three cleanups are behaviour-preserving. `./build_run.sh` reaches ** BUILD SUCCEEDED ** and the full suite is green: `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`.
