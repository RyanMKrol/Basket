## Overview

Focusing the empty add bar shows nothing — prime real estate. Show "your usuals" chips from the
existing `KnownItem` history.

## Do

`Sources/Services/Suggestions.swift` — `combined` currently guards `!q.isEmpty` and returns
nothing for an empty query. Add an explicit "usuals" path: when the add-bar text field is focused
with an EMPTY query, surface up to `combinedMax` chips of the user's most-frequent recent items
(rank by the existing frequency + recency scoring over `KnownItem`, excluding items already on
the list — same exclusion as live suggestions). Wire it through the existing suggestion-chip UI
in `Sources/Views/AddBar.swift` / `ShoppingListView.swift` (`liveSuggestions`), so the chips look
and behave identically to typed suggestions (tap = add). Pure-logic ranking goes in `Suggestions`
so it's unit-testable. Nothing shows for users with no history (fresh install with starter items:
decide seeded starter items count as history only if `KnownItems.rememberAdd` recorded them —
follow existing behaviour, don't special-case). README updated.

## Done when

- Unit tests in `Tests/` pin the usuals ranking (frequency+recency ordering, exclusion of on-list
  items, cap at combinedMax, empty result with no history).
- A new XCUITest: seed history by adding+clearing items, focus the empty add bar, assert usual
  chips appear and tapping one adds it.
- Typed-query suggestions behave exactly as before; full simulator suite green; README updated.
