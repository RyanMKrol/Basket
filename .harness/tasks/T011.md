## Overview

The emoji/suggestion vocabulary is almost entirely food; add household, toiletries, pharmacy,
baby, pet, and brand coverage plus compound-fix entries, with new corpus files to regression-guard
them.

## Do

1. Add compound-fix COMPLEX entries that pre-empt bad simple matches: `toilet paper`,
   `paper towel`, `kitchen paper`, `kitchen towel`, `loo roll` → 🧻; `mince pie` → 🥧;
   `sun cream`, `hand cream`, `face cream` → 🧴; `shaving cream` → 🪒; `salad cream` → 🥫;
   `custard cream`, `bourbon biscuit`, `gingerbread` → 🍪; `cinnamon roll`, `cinnamon bun` → 🥐;
   `carrot cake`, `madeira cake` → 🍰; `rice cake` → 🍘; `ginger beer`, `root beer` → 🥤;
   `dog biscuit` → 🐾; `hearts of palm` → 🥬; `tikka masala` → 🥫 plus simple `masala` → 🧂;
   `fairy liquid` → 🧴 and `fairy cake` → 🧁.
2. Add new simple/complex entries by category (household: bin liner/trash bag/garbage bag → 🧹,
   clingfilm/saran wrap/freezer bag/ziplock/parchment paper → 🧻, light bulb → 💡, air freshener/
   washing powder/laundry pod/stain remover/dryer sheet/toilet cleaner/descaler/dettol/zoflora/
   febreze/windex/clorox/lysol → 🧴, dish soap → 🧼, mop/dustpan → 🧹; toiletries: suncream/
   sun lotion/spf/sanitizer/hand gel/lip balm/chapstick/vaseline/body wash/bubble bath/bath bomb/
   makeup remover/micellar/cotton pad/aftershave/dry shampoo/hair dye → 🧴, face wash → 🧼,
   shave gel → 🪒, nail polish/nail varnish → 💅, wet wipe → 🧻; pharmacy: aspirin/tylenol/advil/
   calpol/nurofen/lemsip/gaviscon/rennie/antihistamine/piriton/benadryl/strepsils/lozenge/
   hay fever/allergy/multivitamin/probiotic → 💊, thermometer/pregnancy test → 🩺; baby:
   pacifier/dummy/dummies/teething/baby oil/bib/pampers/huggies/baby milk → 🍼, rusk → 🍪; pet:
   kibble/dog treat/cat treat/catnip/poo bag/flea treatment/whiskas/felix/pedigree/dreamies → 🐾,
   bird seed/birdseed → 🐦, fish food → 🐠; brands/drinks: pepsi/coke/dr pepper/fanta/sprite/
   irn bru/lucozade/red bull/pop → 🥤, ribena → 🧃, colgate → 🪥, nescafe → ☕, pg tips/
   yorkshire tea/twinings/kombucha → 🍵, lurpak/flora → 🧈, dairylea/philadelphia → 🧀,
   andrex/cushelle → 🧻, protein bar → 🍫, hot cross bun/banana bread → 🍞, fries/chip → 🍟).
3. Create 5 new corpus files (`tools/corpus/household_corpus.txt`, `toiletries_corpus.txt`,
   `pharmacy_corpus.txt`, `baby_pet_corpus.txt`, `brands_uk_us_corpus.txt`) listing realistic
   items per category, so the coverage audit regression-guards them and
   `tools/gen_suggestions.py` folds them into autocomplete.
4. Run the full pipeline: regenerate BOTH generated files, re-run the coverage audit.

## Done when

- All 5 new corpus files exist and the coverage audit reports 0 fall-throughs across the ENLARGED
  corpus.
- Spot-checks through the real cascade: "toilet paper" → 🧻, "loo roll" → 🧻, "bin liners" → 🧹,
  "clingfilm" → 🧻, "kibble" → 🐾, "pepsi" → 🥤, "calpol" → 💊, "dummies" → 🍼.
- Generated files regenerated (not hand-edited); native harness + full simulator suite pass;
  README's emoji-cascade description still accurate.
