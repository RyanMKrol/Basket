## Overview
Re-attempt of T031. The original was declared blocked purely for scope-creep: the widget extension's Swift files live under BasketWidget/ (built by T033, the now-done view-only widget), but T031's scope listed only Sources/, Tests/, project.yml, README.md — so when the agent correctly added the new widget-variant files under BasketWidget/ the scope gate rejected the diff. Both real dependencies (T028 basket://add deep link — done; T033 widget foundation — done) are ready; this is a scope-authoring fix, not new difficulty.

## Do
Building on the shipped basket://add deep link (handled in Sources/BasketApp.swift's onOpenURL) and the existing widget extension (BasketWidget/BasketWidget.swift + BasketWidgetBundle.swift, wired in project.yml):
1. Add a '+' add affordance to the widget that opens the app via the basket://add deep link using widgetURL(...) / Link, so tapping it lands in the app with the add bar focused and the keyboard up (you cannot type into a widget itself — this is the intended 'take me into the app to add' behaviour).
2. Provide the variants the owner wants, selectable from the WidgetBundle: the existing view-only list widget (keep BasketWidget as-is), a compact small 'add' widget that is mostly the '+' quick-add button, and — where it fits the medium layout — a combined widget that shows the list AND carries the '+' button. Add the new widget structs to BasketWidgetBundle.body so all variants appear in the gallery.
3. Match the Pastel Dots look (reuse Theme / containerBackground(Theme.paper, for: .widget) as the existing entry view does); handle small + medium sizes.
Put new widget Swift files under BasketWidget/ (that directory is now in scope). If a new widget kind identifier is needed, add it to Sources/Support/BasketWidgetKind.swift alongside the existing one so the extension and any app-side reload stay in sync. Regenerate the project (xcodegen) after adding files.
Note: widget rendering isn't XCUITest-able and the deep-link landing (keyboard up) is already covered by T028's app-side test; this task's automated bar is compile + a small unit test asserting the '+' widgetURL resolves to basket://add.

## Done when
The widget bundle builds with the view-only, add, and combined variants all present in BasketWidgetBundle.body (selectable in the gallery); the '+' button's widgetURL is the basket://add deep link, asserted by a new unit test in Tests/ that checks the constructed URL equals basket://add (scheme 'basket', host 'add'); README documents the three widget variants; xcodegen-generated project builds and the full simulator suite plus native harness stay green.
