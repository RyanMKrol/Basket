## Overview
Bake in the single background-blur intensity the owner chose in T077 and remove the temporary comparison scaffolding T076 added. The focus-driven background-blur behaviour stays; only the switch and the unused levels go, so the evaluation scaffolding doesn't become permanent cruft.

## Do
In `Sources/Views/` (`ShoppingListView.swift` and wherever T076 placed the `BlurStyle` switch): hardcode the ONE blur intensity the owner selected in T077, and delete the temporary runtime switch / debug segmented control and the two unused intensity levels. If `BlurStyle` is now a single value, collapse it to a plain constant. Keep the focus-driven blur behaviour itself (blur the background content while the add bar + suggestions stay sharp), animated via `withAppAnimation` / `.unlessUITesting`. The concrete chosen value is recorded in T077's worklog — read it from there.

## Done when
- The background blur uses exactly the owner-chosen intensity, with NO runtime switch and NO unused blur levels remaining (`grep -rn "BlurStyle" Sources/Views/` shows either nothing or a single collapsed constant, and the temporary debug control is gone).
- The focused-state background blur still works (list recedes, add bar + suggestions sharp), disabled under UI testing.
- `./build_run.sh` reaches `** BUILD SUCCEEDED **` and the full simulator suite is green.
- Visual verification: capture the focused state showing the final chosen blur.
