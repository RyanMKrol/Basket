## Overview
The scheduled nightly flake-hunt job in .github/workflows/ci.yml reruns the whole test suite up to 5 times to surface flaky tests, uploading an .xcresult artifact on failure. The owner has decided nobody ever looks at these results and the code only changes while they are actively running the project, so the job is pure waste: remove it entirely rather than making it more actionable.

## Do
In .github/workflows/ci.yml: delete the flake-hunt job (currently around line 94-126, including its 'Run the suite repeatedly to surface flakes' step and the flake-hunt-results artifact upload), and remove the schedule trigger if flake-hunt was its only consumer (check the comment around line 8 which says the schedule drives the flake-hunt job only; if nothing else uses on.schedule, delete that trigger block too). Keep the push-triggered test job and its -retry-tests-on-failure -test-iterations 2 retry policy exactly as they are. Then update the prose that documents the deleted job: CLAUDE.md (the 'Definition of done' section around line 77 describes the scheduled nightly flake-hunt job rerunning every test up to 5 times) and README.md (around line 309, same description) so neither claims the job still exists; keep their surrounding explanation of the retry-once policy intact.

## Done when
grep -i 'flake-hunt' across .github/workflows/ci.yml, CLAUDE.md, and README.md returns no hits; the ci.yml on: block no longer contains a schedule trigger unless something other than flake-hunt consumed it; the push-triggered CI job is untouched (retry policy intact) and goes green on the branch push. No test or source files change.
