## Overview

Delete ~1,900 lines of superseded icon/art generator tools that nothing references.

## Do

Delete these 12 dead files from `tools/` (CLAUDE.md already marks the icon renderers
"superseded" — the shipping icon is agency artwork; none are referenced by README, CI, or any
script): `make_icon.swift`, `make_icon_b.swift`, `make_icon_concepts.swift`,
`make_icon_explore.swift`, `make_icon_flat.swift`, `make_icon_options.swift`,
`make_icon_styles.swift`, `make_icon_variants.swift`, `make_basket_art.swift`,
`make_basket_pixel.swift`, `montage.swift`, `probe_embedding.swift`.
KEEP: `main.swift`, `audit_coverage.swift`, `gen_emoji.py`, `gen_suggestions.py`,
`emoji_supplement.txt`, `corpus/`, `export_ui_screenshots.sh`, `rename_ui_screenshots.py`.
Update the `tools/` section of CLAUDE.md's project map (remove the superseded-icon-renderer
bullet) and any README mention in the same commit. Git history preserves the deleted files.

## Done when

- The 12 files are gone; `grep -rn 'make_icon\|make_basket\|montage.swift\|probe_embedding'
  README.md CLAUDE.md .github/ build_run.sh project.yml` returns nothing.
- CLAUDE.md's project map reflects the surviving tools only.
- Full simulator suite + native harness still green (nothing compiled these, so this is a
  docs-consistency check).
