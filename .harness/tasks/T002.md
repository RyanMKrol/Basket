## Do
In Sources/Views/ShoppingListView.swift, function `celebrateCleared()` (around lines 385-392), the exit line currently reads `DispatchQueue.main.asyncAfter(deadline: .now() + 1.6) { withAnimation(.easeIn(duration: 0.4)) { celebrating = false } }`. `celebrating` gates the `ClearedCelebration` overlay shown at the `if celebrating && !listIsEmpty { ClearedCelebration(reduceMotion: reduceMotion).transition(.opacity) }` site (around lines 115-118), so this withAnimation call is what should produce the fade-out. The bug: `.easeIn` is the wrong curve for a disappearing element — it holds near-full opacity for most of the duration and only drops rapidly at the very end, which reads as an abrupt clip/cut rather than a gradual fade, matching the reported symptom. This codebase already has the correct, established precedent for dismissing a near-identical full-screen celebratory overlay: `LaunchFlourish`'s dismissal in the same file (around lines 133-140) uses `withAnimation(.easeOut(duration: 0.4)) { showFlourish = false }` paired with its own `.transition(.opacity)`. Change the celebration's exit animation from `.easeIn(duration: 0.4)` to `.easeOut(duration: 0.4)` to match that convention exactly. Do not change the 1.6s hold delay, the entrance animation (`withAnimation(.easeOut(duration: 0.3)) { celebrating = true }`), the `.transition(.opacity)` modifier itself, or anything in ClearedCelebration.swift/SparkleBurst.swift.

## Done when
The exit `withAnimation` call inside `celebrateCleared()` in ShoppingListView.swift uses `.easeOut(duration: 0.4)` (matching LaunchFlourish's dismiss pattern in the same file) instead of `.easeIn(duration: 0.4)`. Checking off the last remaining item and observing the celebration in the simulator shows a smooth, gradual opacity fade-out at the end of its 1.6s hold rather than an abrupt disappearance. No other timing constants, curves, or files are touched.
