## Overview
Re-attempt of T070, which was ITSELF a re-attempt of T044. T044 hard-blocked on scope-creep (tools/main.swift not in scope). T070 fixed the scope but then blocked AGAIN — this time identically across all 4 attempts (sonnet x2 AND opus x2, escalated to the top model rung), never a scope or CI issue. The NEW root cause is a genuine gap in the migration guidance, not scope or model capability: tools/main.swift is a Swift *top-level executable script*, and under `swift-version 6` its module-scope code runs in a NONISOLATED synchronous context (top-level code is only implicitly @MainActor under Swift's normal `-parse-as-library`-off single-main-file rule, which does NOT apply the same way once other actor-isolated symbols enter — and the harness's swiftc line compiles many files). So the instant the migration marks a service enum the harness touches (or a local `check`/`seasonDate` helper) `@MainActor`, ALL ~200 top-level `check(...)` calls become 'call to main-actor-isolated function from a nonisolated synchronous context' — a hard error that CANNOT be fixed by annotating individual helpers (that just moves the error to every call site, which is exactly the wall every attempt hit). The spec's `## Do` never says how to make tools/main.swift itself main-actor-safe under Swift 6.

## Do
Do the same behaviour-preserving Swift 6 migration T070 described (SWIFT_STRICT_CONCURRENCY: complete first under 5.0, fix LaunchOnce/AddToBasketIntent/Emoji/SemanticEmoji/Haptics unsafe statics per T070's guidance, then flip SWIFT_VERSION to 6.0), PLUS the missing piece: make the native harness main-actor-safe. Wrap tools/main.swift's top-level code in a `@main struct BasketCheck { static func main() { ... } }` (or an equivalent `@MainActor`-run entry point) and compile the harness with `-parse-as-library`, so the whole script body runs in a single well-defined actor context and can call whatever the migration isolates. Update BOTH the .github/workflows/ci.yml 'Native logic harness' step AND the README.md Tests-section command to add `-swift-version 6 -parse-as-library` to the swiftc invocation, so the local command, CI, and the harness header comment all match. Keep every `check(...)` assertion and its message exactly as-is (no test weakened, skipped, or deleted). Any surviving `nonisolated(unsafe)` in Sources/ still needs an adjacent justification comment plus a README row, per T070. Do NOT edit any .harness/ file.

## Done when
project.yml settings.base has SWIFT_VERSION: "6.0" and `xcodegen generate && ./build_run.sh` reaches ** BUILD SUCCEEDED ** with zero concurrency errors. `grep -rn 'nonisolated(unsafe)' Sources/` is empty OR every occurrence carries an adjacent justification comment with a matching README row. The native harness compiles AND passes under Swift 6 via `swiftc -swift-version 6 -parse-as-library <the 9 Sources files> tools/main.swift -o /tmp/basket_check6 && /tmp/basket_check6` (exit 0, prints ALL PASSED), and ci.yml's 'Native logic harness' step + README's harness command both use the identical `-swift-version 6 -parse-as-library` flags. Full simulator suite green (`./tools/loop_sim.sh >/dev/null && xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=Basket-Claude'`), including the adjusted AddToBasketIntentTests. No behaviour change: no new tests, nothing in the existing suite weakened or removed.
