## Do
Two linked fixes to Sources/Views/ShoppingListView.swift and Sources/Views/QuantityEditor.swift so the inline quantity editor (opened via ItemRow's '+ Qty' chip / row tap, tracked by the single `@State private var expandedID: PersistentIdentifier?` in ShoppingListView) can be dismissed from outside itself: (1) In ShoppingListView.swift, the top-level `var body` starts with `ZStack { BasketBackground(); VStack(spacing: 0) { header; if listIsEmpty { ... } else { ScrollView { ... } } } }` (roughly lines 57-119), immediately followed by `.safeAreaInset(edge: .bottom) { AddBar(...) }` (line ~120). Add `.contentShape(Rectangle())` and `.onTapGesture { ... }` modifiers on that outer ZStack, placed right after its closing brace and before `.safeAreaInset`. The closure should do nothing when `expandedID == nil`, and otherwise close the editor exactly like the close branch of the existing `toggleQuantityEditor(_:)` function (lines ~281-300) does: `withAnimation(.spring(response: 0.35, dampingFraction: 0.82)) { expandedID = nil }`. Do not add `Haptics.soft()` for this dismiss (that call in `toggleQuantityEditor` is tied to the explicit open/close action, not an outside tap). Because SwiftUI resolves gestures on the most deeply-nested view first, this new ZStack-level tap only fires for taps that land in space no descendant view already claims with its own gesture (e.g. blank space in the ScrollView below the last row, or the header/background margins) — ItemRow's own `.onTapGesture` (ItemRow.swift line 84), the CheckCircle's tap (line 74), the quantity editor's own controls (QuantityEditor.swift stepButton/unitPill/onClear/valueDisplay taps), and the header/title taps in ShoppingListView all keep working unchanged because those views' own gestures win for the area they cover. Because `BasketBackground` (Sources/Theme/Theme.swift, ~line 112) paints an opaque `Theme.paper` `Color` first, the ZStack already has a full-bleed hit target across the whole screen; the added `.contentShape(Rectangle())` just makes that explicit and robust. (2) In Sources/Views/QuantityEditor.swift, the exact-amount `TextField` inside `valueDisplay`'s `editing` branch (lines ~60-82) currently has no way to dismiss its keyboard on its own (per the code's own comment at lines 76-78, tapping the X, the row, or another row are today's only ways to end editing — there is deliberately no on-keyboard Done button, per commit 72dd9fb, because a `.toolbar(placement: .keyboard)` accessory was tried before and overlapped the bottom of the list). Do NOT re-add that toolbar/accessory. Instead add `.submitLabel(.done)` and `.onSubmit(commit)` to the same `TextField` (the private `commit()` function already at lines ~125-130 is exactly what focus-loss already calls via `.onChange(of: fieldFocused)`, so reuse it) — this costs nothing and commits+resigns focus for any keyboard configuration that does present a Return/Done key (e.g. an attached hardware keyboard), while the ZStack-level outside-tap fix from (1) is what actually satisfies dismissing the keyboard on `.numberPad`/`.decimalPad` (which never show an on-screen Return key), since collapsing `expandedID` removes the QuantityEditor (and its focused TextField) from the view hierarchy, which resigns the keyboard along with it. Finally, update the README.md 'Tap the row... to set a quantity' bullet (~lines 30-42) to mention that tapping elsewhere (blank space, another row, etc.) closes an open quantity editor and dismisses its keyboard.

## Done when
Tapping outside an open quantity editor (blank scroll space, the header, or any area not owned by a row/button) closes that editor via `expandedID = nil`, regardless of whether its keyboard is currently up; the exact-amount TextField also carries `.submitLabel(.done)` + `.onSubmit(commit)`. All pre-existing tap targets (row body/chip toggle, CheckCircle, −/+ steppers, unit pills, the X clear button, switching between rows, Got-it row restore, header/title/info taps, the add bar) behave exactly as before. README.md's quantity bullet documents the new outside-tap dismiss behavior.
