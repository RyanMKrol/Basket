## Overview

`flash(_:)` reads a SwiftData model 0.9 s after scheduling; the item can be deleted in that window.

## Do

In `Sources/Views/ShoppingListView.swift`, `flash(_:)` (~line 448) schedules a
`DispatchQueue.main.asyncAfter` closure that reads `item.persistentModelID` after ~0.9 s. The item
can be deleted in that window (the 60 s TTL purge tick, or a "Clear all" after a rapid
check-off + re-add), making this the one place the app touches a possibly-deleted `@Model`.
Capture `let id = item.persistentModelID` BEFORE scheduling the closure and compare the captured
`id` inside the closure (e.g. `if flashID == id { flashID = nil }`), so the closure never touches
the model object itself.

## Done when

- The delayed closure in `flash(_:)` references only a `PersistentIdentifier` captured before
  scheduling — no access to the `GroceryItem` instance inside the closure.
- Behaviour is unchanged: re-adding a duplicate item still flashes its row and the flash still
  clears (`AddItemFlowTests` remains green).
- The full simulator suite passes (`xcodegen generate && xcodebuild test -project Basket.xcodeproj
  -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`).
