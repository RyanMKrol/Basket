## Do
In Sources/Views/ItemRow.swift, the row's whole-body gesture at the `.onTapGesture { showsQuantity ? onTapQuantity() : onToggle() }` line (in the outer HStack's modifier chain, right after `.contentShape(Rectangle())`, just above `if isExpanded, let quantityEditor {`) currently calls `onToggle()` for any tap on a Got-it row's body (showsQuantity is false for Got-it rows, as passed from ShoppingListView's `recentlyGot` ForEach which constructs ItemRow without setting showsQuantity, so it defaults to false) — this is the reported bug: tapping ANYWHERE on a checked-off row's body unmarks it, not just the check circle. Change that body `.onTapGesture` closure so the got-it (showsQuantity == false) branch does nothing instead of calling onToggle() — e.g. `.onTapGesture { if showsQuantity { onTapQuantity() } }` — so unmarking only ever happens via the CheckCircle's own `.onTapGesture(perform: onToggle)` (a few lines above, on the `CheckCircle(...)` view with its dedicated `.frame(width: 40, height: 40).contentShape(Rectangle())` tap target). Do NOT touch the to-get row path: showsQuantity is always true there, so `onTapQuantity()` (opening the inline quantity editor) must keep firing on a to-get row's body tap exactly as today. Do NOT change the row-level `.accessibilityAction { showsQuantity ? onTapQuantity() : onToggle() }` / `.accessibilityHint(..."Double tap to restore to your list")` block a few lines above (around the `.accessibilityElement(children: .ignore)` group) — that is a deliberate, separate VoiceOver affordance (the code's own comment above the body `.onTapGesture` already says "Touch only — VoiceOver uses the accessibility actions above instead"), giving VoiceOver users a larger whole-row restore target since precise tapping is harder for them; this bug report is specifically about the sighted/touch tap-anywhere behavior, so VoiceOver's restore-by-double-tap-on-row stays as is. Update the ItemRow.swift file-header doc comment (the `///` block at the top describing tap targets, which currently says "In the faded \"Got it\" section there's no quantity affordance, so tapping anywhere restores the item.") to describe the new behavior: only the check circle restores a Got-it row. Also fix README.md's Features bullet ("**Tap the check circle** to check an item off — ... then the row glides into a dimmed **\"Got it\"** section (tap it to restore it).") — change "(tap it to restore it)" to make clear restoring requires tapping the check circle specifically, not the row.

## Done when
Tapping any part of a Got-it row's body other than the check circle (e.g. the item name, emoji, or empty space) no longer calls onToggle/unmarks the item — only tapping the CheckCircle does. A to-get row's body tap still opens its quantity editor and its check circle still checks it off, unchanged. The ItemRow.swift header doc comment and the README.md 'Tap the check circle...' bullet both accurately describe the new check-circle-only restore behavior. VoiceOver's row-level accessibility action/hint for restoring a Got-it item is left untouched.
