## Overview
In Sources/Views/ItemRow.swift, a 'Got it' (checked-off) row currently unmarks itself when the user taps anywhere on the row body, not just the CheckCircle — this is the current intended behavior per the doc comment (lines 1-10) and the body's onTapGesture at line 84 (`showsQuantity ? onTapQuantity() : onToggle()`, where got-it rows have showsQuantity == false so any body tap calls onToggle()). Change this so a got-it row's body tap does nothing; only tapping the CheckCircle (lines 70-78, which already has its own onTapGesture(perform: onToggle) and accessibility action) unmarks it.

## Do
In Sources/Views/ItemRow.swift, change the body onTapGesture at line 84 so the else-branch no longer calls onToggle() for got-it rows — it should become a no-op, e.g. `.onTapGesture { if showsQuantity { onTapQuantity() } }` (to-get rows keep opening the quantity editor exactly as before). For accessibility parity, also update the row-body accessibility element (lines 61-68): for got-it rows (showsQuantity == false) it should no longer expose the `.isButton` trait, the `accessibilityAction { onToggle() }`, or the 'Double tap to restore to your list' hint, since that would leave VoiceOver users an affordance sighted/touch users no longer have. CheckCircle's own dedicated accessibility element (lines 75-78, label 'Got it'/'Not got yet', with its own accessibilityAction calling onToggle()) becomes the sole restore control for both touch and VoiceOver — leave it untouched. Update the doc comment at the top of the file (lines 1-10) to describe the new behavior: in the 'Got it' section, only the check circle restores the item; tapping the rest of the row does nothing.

## Done when
Tapping anywhere on a got-it row's body outside the check circle has no effect — the item stays checked off; tapping (or VoiceOver double-tapping) the CheckCircle still unmarks it exactly as before. To-get row behavior (body tap opens the quantity editor, hint 'Double tap to set quantity') is unchanged. A got-it row's info accessibility element no longer carries the isButton trait, an accessibilityAction, or a 'restore' hint; VoiceOver users can only restore via the CheckCircle's own 'Got it' element and its double-tap action. The doc comment at the top of ItemRow.swift matches this new behavior. xcodegen generate && xcodebuild build (or ./build_run.sh) succeeds and the app installs/launches on the simulator with checked-off items still displaying correctly in the faded 'Got it' section.
