## Overview

`ShoppingListView` is 462 lines doing layout + quantity plumbing + check-off choreography wiring +
duplicate-add + purge + celebration. Decompose it without changing behaviour.

## Do

Refactor `Sources/Views/ShoppingListView.swift` to shrink the root view, extracting cohesive pieces
into their own types/subviews under `Sources/Views/` (new files are fine — add them and run
`xcodegen generate` so they're picked up):

- Extract the "to get" and "Got it" section `ForEach` blocks into their own section subviews.
- Extract the five quantity handlers (`toggleQuantityEditor`, `stepQuantity`, `pickQuantityUnit`,
  `setQuantity`, `clearQuantity`, ~lines 289–346) into a small helper type that operates on a
  `GroceryItem`.

This is a **pure refactor**: no user-visible behaviour change, no change to the check-off
choreography, duplicate-add flash, TTL purge, or celebration. Keep the existing accessibility
identifiers and the `TestHooks` / `AppClock` / `withAppAnimation` discipline intact so all UI tests
still pass unchanged. Prefer moving code over rewriting it.

## Done when

- `ShoppingListView.swift` is materially smaller, with the section rendering and quantity handlers
  extracted into their own well-named types/subviews; no behaviour changed.
- Every existing test passes **without modification** (the refactor must not require touching test
  assertions): full simulator suite green (`xcodegen generate && xcodebuild test -project
  Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`),
  including the check-off, quantity, and celebration UI-test flows.
- The app looks and behaves identically (verify via `build_run.sh` screenshot).
