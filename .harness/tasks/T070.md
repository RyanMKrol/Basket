## Overview
Re-attempt of T044, which was blocked (scope-creep, single attempt, no escalation): its `## Do` and `## Done when` require editing tools/main.swift (the Swift 6 native-harness compile is an acceptance bar) but tools/ was missing from scope, so the loop hard-blocked on the first out-of-scope edit. The only change here is scope now includes tools/main.swift; the migration itself is unchanged and small (one nonisolated(unsafe) static in LaunchFlourish plus a handful of AppIntents/service statics).

## Do
Behaviour-preserving migration to Swift 6 language mode, in two steps within this one task.

STEP 1 — surface diagnostics under Swift 5 mode first. In project.yml's settings.base add `SWIFT_STRICT_CONCURRENCY: complete` (keep SWIFT_VERSION "5.0" for now), run `xcodegen generate`, build, and fix every warning. Known fixes:
- LaunchOnce (Sources/Views/LaunchFlourish.swift): mark the enum @MainActor and drop `nonisolated(unsafe)` from `fired` (only touched from SwiftUI view code); keep the doc comment's intent.
- AddToBasketIntent.containerOverride (Sources/Services/AddToBasketIntent.swift): read only from the already-@MainActor resolveContainer() — mark it @MainActor and annotate Tests/AddToBasketIntentTests.swift (sets it ~lines 17/23/43) @MainActor as needed; confirm Tests/TipJarTests.swift still compiles.
- Same file: convert static var typeDisplayRepresentation / defaultQuery / title / description to `static let` or computed `static var { ... }` (the AppIntents protocols only require { get }).
- Emoji.cache (Sources/Services/Emoji.swift) and SemanticEmoji.embedding (Sources/Services/SemanticEmoji.swift): NSCache is documented thread-safe and NLEmbedding is not Sendable — either isolate the enum to @MainActor OR keep the static global with `nonisolated(unsafe)` plus an adjacent justification comment citing the external synchronization. Pick ONE approach and apply it consistently to both. Whichever you pick, these two files must still compile in a plain macOS `swiftc -swift-version 6` invocation with no UIKit assumptions (they are in the native-harness compile list), so do NOT introduce UIKit-only isolation into them. `@preconcurrency import NaturalLanguage` may be needed in SemanticEmoji.
- Haptics (Sources/Services/Haptics.swift): UIFeedbackGenerator is MainActor-isolated in modern SDKs — mark the enum @MainActor (all call sites are views/gestures on the main actor).
- Chase remaining diagnostics (e.g. DispatchQueue.main.asyncAfter closures in ShoppingListView.swift/LaunchFlourish.swift capturing view state; TipJar's `updatesTask = Task { [weak self] ... }`). Prefer @MainActor isolation / structured alternatives over `nonisolated(unsafe)`; any surviving `nonisolated(unsafe)` MUST carry an adjacent justification comment.

STEP 2 — flip the mode. Change project.yml SWIFT_VERSION to "6.0" (the now-redundant SWIFT_STRICT_CONCURRENCY key may be removed), regenerate, and fix anything that upgraded from warning to error.

CI + harness alignment: the .github/workflows/ci.yml "Native logic harness" step compiles shared Sources files with plain swiftc (defaults to Swift 5). Add `-swift-version 6` to that swiftc invocation and edit tools/main.swift as needed so the native harness compiles and runs cleanly under Swift 6 (this is the edit that was previously out of scope). Update the matching native-harness command in README.md (Tests section) and any other README mention of the Swift language mode. If any `nonisolated(unsafe)` survives with justification, add a matching row to README.md in the same commit. Do NOT edit any .harness/ file in this task.

## Done when
- project.yml settings.base has `SWIFT_VERSION: "6.0"` and `xcodegen generate && ./build_run.sh` reaches ** BUILD SUCCEEDED ** with zero concurrency errors.
- `grep -rn 'nonisolated(unsafe)' Sources/` is empty, or every remaining occurrence has an adjacent justification comment (external synchronization, e.g. NSCache) with a matching row in README.md.
- Full simulator suite green: `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` passes (XCTest + XCUITest, including the adjusted AddToBasketIntentTests).
- Native harness compiles and passes under Swift 6: `swiftc -swift-version 6 Sources/Services/Emoji.swift Sources/Services/EmojiTable.swift Sources/Services/SemanticEmoji.swift Sources/Services/Suggestions.swift Sources/Services/SuggestionDictionary.swift Sources/Models/Suggestion.swift Sources/Services/Formatting.swift Sources/Services/Measure.swift Sources/Services/Seasonality.swift tools/main.swift -o /tmp/basket_check && /tmp/basket_check` succeeds, and .github/workflows/ci.yml's "Native logic harness" step uses the same `-swift-version 6` flag (README.md's harness command kept in step).
- No behaviour change: no new tests expected, and nothing in the existing suite is weakened, skipped, or deleted to reach green.
