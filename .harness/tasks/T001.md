## Do

Add a short "Harness dashboard" subsection to `README.md`'s "Build & run" section documenting
that `node .harness/dashboard/server.js` starts a local read-only viewer for the
`.harness/tracking/TASKS.json` backlog (task status, dependencies, recent worklog). Mechanical
doc-only change — this is the first task run through the loop, so its job is to prove the
pipeline (build → local DoD → CI → merge) end-to-end on something trivial and zero-risk.

## Done when

`README.md` documents the dashboard command in its "Build & run" section; the existing Definition
of Done commands (XCTest suite + native logic harness) still pass unchanged, since this task
touches no Swift source.
