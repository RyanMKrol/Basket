## Do

In `tools/main.swift`, the check labelled "empty-state line stable within a day" (near the end of
the file, around lines 197–198) compares `Seasonality.emptyStateLine(seasonDate(2026, 3, 10),
calendar: utc)` against the **identical expression at the identical instant** — it can never fail
and therefore tests nothing.

Replace it with a real assertion of the property it names: the empty-state line is stable within
a calendar day but rotates across days. Concretely:

1. Call `Seasonality.emptyStateLine` for **two different times on the same day** (e.g. 08:00 and
   22:00 on 2026-03-10, via the existing `seasonDate` helper — extend it with an hour parameter if
   it only takes year/month/day) and assert the two results are **equal**.
2. Add a second check that the line rotates across days: because adjacent days may occasionally
   collide depending on the rotation, sample 2–3 different fixed days (e.g. 2026-03-11 and
   2026-03-15) and assert at least one differs from the 2026-03-10 line. Keep the check fully
   deterministic — fixed dates, fixed calendar, no live clock.

Keep the harness's existing `check(_:_:)` reporting style and comment conventions. Do not change
`Sources/Services/Seasonality.swift` — this is a test-only fix.

## Done when

- The tautological self-comparison at the "empty-state line stable within a day" check is gone,
  replaced by the same-day-equality check plus a cross-day-difference check, both using fixed
  deterministic dates.
- The native logic harness compiles and passes: build the `Sources` pure-logic files together with
  `tools/main.swift` (see README.md → Tests) and run it — all checks green.
- The full test suite still passes (`xcodegen generate && xcodebuild test -project
  Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`).
- CI is green on the task branch.
