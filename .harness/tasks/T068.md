## Overview
Re-attempt of T035, which was blocked by scope-creep: the spec told the builder to create tools/semantic-anchor-report.txt and gitignore it, but the task's scope array listed ONLY tools/audit_coverage.swift, so writing the required report file (and there was no .gitignore entry to make it untracked) tripped the loop's scope gate. The investigation work itself is unchanged and still needed — the -semantic-report mode does not yet exist and the vinegar/sauce/ketchup/mustard -> 🥫 anchor cluster is still live in SemanticEmoji.swift. This unit widens scope to cover the deliverable and the gitignore entry.

## Do
Extend tools/audit_coverage.swift with a new report mode (a `-semantic-report` flag wired to match the file's existing flag-handling pattern for `-correctness`, i.e. via CommandLine.arguments) that, for every word in tools/corpus/*.txt which falls through EmojiTable.match to SemanticEmoji.match, records: the input word, the nearest anchor word it matched, the similarity score, and the resulting glyph. Group and print the output by resulting glyph so glyphs claimed by multiple semantically-unrelated anchor words (like 🥫's vinegar/sauce/ketchup/mustard cluster) are easy to spot. Write the full report to tools/semantic-anchor-report.txt AND print a summary to stdout. Add a .gitignore entry for tools/semantic-anchor-report.txt so this diagnostic artifact stays untracked (it is a generated diagnostic, not curated data) — this is why .gitignore is in scope. Do NOT change SemanticEmoji.swift's anchor list or threshold, EmojiTable.swift, or tools/gen_emoji.py: this is read-only investigation tooling; deciding whether any specific collision is wrong and fixing it is a separate owner task. The ONLY behavioural changes are the new flag path in audit_coverage.swift plus the .gitignore line; the existing default and `-correctness` modes must be byte-for-byte unaffected.

## Done when
Running `swift tools/audit_coverage.swift -semantic-report` produces tools/semantic-anchor-report.txt listing every corpus word that falls through to the semantic stage, grouped by resulting glyph, each entry showing the matched anchor word and similarity score. The report surfaces the known vinegar/sauce/ketchup/mustard -> 🥫 cluster as a multi-anchor group (proving the tool works). Running the tool with no flag, and with `-correctness`, produces the same output as before this change. tools/semantic-anchor-report.txt is gitignored (git status shows it untracked/ignored, so no scope-creep). No changes to Sources/Services/SemanticEmoji.swift, Sources/Services/EmojiTable.swift, or tools/gen_emoji.py.
