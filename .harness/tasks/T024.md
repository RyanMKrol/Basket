## Overview

Two small formatting-consistency cleanups the review flagged: a few over-long lines, and MARK usage
that's present in some files but not others.

## Do

Cosmetic only — no behaviour changes:

1. **Wrap the over-long lines** the review named: `Sources/Views/ShoppingListView.swift:103`
   (the `.animation(.spring…, value: items.map(\.isChecked))` line), `Sources/Services/TipJar.swift`
   (~lines 70–73, the logger lines), and `Sources/BasketApp.swift:26`. Break them across lines in the
   idiomatic Swift way (leading-dot chains one modifier per line, wrapped argument lists), matching
   the surrounding style. If other lines in these same files are egregiously long, wrap them too, but
   keep the change small and mechanical.
2. **Make MARK usage consistent.** Some multi-section files use `// MARK:` dividers and others
   (`Sources/Services/Measure.swift`, `Sources/Views/QuantityEditor.swift`,
   `Sources/Views/AboutView.swift`) don't. Adopt one convention: **add `// MARK:` section dividers to
   the files over ~100 lines that lack them**, matching the style already used elsewhere in the repo
   (e.g. `Theme.swift`). Do not reorder or move code — only add the section markers.

## Done when

- The named long lines are wrapped and no longer exceed a reasonable width (~130 cols); the wrapping
  is purely cosmetic (identical behaviour).
- The listed multi-section files carry `// MARK:` dividers consistent with the rest of the codebase.
- No logic changes anywhere; full simulator suite green (`xcodegen generate && xcodebuild test
  -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`)
  and the native harness still green.
