## Overview
Re-attempt of T043 (blocked). The original blocked on the scope gate at attempt 0: the spec's `## Do` step 5 instructed the builder to add a row to `.harness/custom/docs/LIMITATIONS.md` IN THE SAME COMMIT, but the task's scope only declared `tools/`, `README.md`, `.gitignore`. The spec was self-contradictory (the same step also said 'do NOT edit any .harness/ file ... the owner records the .harness LIMITATIONS.md row separately'), and the model followed the literal 'add one row' and tripped the first out-of-scope edit before any real work landed. This re-attempt removes the .harness LIMITATIONS instruction from the buildable task entirely (the owner records that row separately, per the harness's own rule that the loop is the sole writer of task status and .harness/custom is off-limits during loop-driven work), leaving a clean tooling+docs task inside its declared scope. The profiling work itself is real and unbuilt (no tools/profile_app.sh, no tools/perf-baseline/, no README perf section) and xcprof is present on this machine at the path the spec names.

## Do
This is tooling + developer-docs only. Touch ONLY tools/, README.md, and .gitignore. Do NOT edit any .harness/ file, app code, project.yml, Tests/, UITests/, or .github/workflows/ci.yml. Do NOT change any TASKS.json status.

1. Create `tools/profile_app.sh` (executable, `set -euo pipefail`), modeled closely on the existing `tools/record_ui_test.sh` (read it plus `build_run.sh` first for the repo-root resolution, the simulator name -> UDID resolution, and the background-side-process + `trap cleanup EXIT` pattern). Usage: `tools/profile_app.sh [test-identifier] [simulator-name] [output-label]`.
   - `--help`/`-h` MUST print usage and exit 0 BEFORE any xcprof lookup, so help works on a machine without the Axiom plugin (this is in verify).
   - Default workload: `BasketUITests/SuggestionsFlowTests/testTappingSuggestionChipAddsItem` (single app launch exercising typing -> suggestion scoring -> emoji lookup -> add). The test-identifier arg overrides it.
   - Locate xcprof with graceful degradation: try `command -v xcprof`; else glob `$HOME/.claude/plugins/cache/axiom-marketplace/axiom/*/bin/xcprof` and pick the highest version (`sort -V | tail -1`) — the version present today is `$HOME/.claude/plugins/cache/axiom-marketplace/axiom/27.0.0-beta.22/bin/xcprof`. If none is found, print an actionable message naming that plugin-cache path and exit 2 (never a silent or cryptic failure).
   - Run `xcprof doctor` once; if xctrace is unavailable, fail fast (exit 2, clear message).
   - Record around the workload: `xcodegen generate`, then start `xcodebuild test -project Basket.xcodeproj -scheme Basket -destination "platform=iOS Simulator,id=$SIM" -only-testing:"$TEST_ID"` in the background; poll (bounded ~60s, with sleep) for the host-visible Basket process (`pgrep -x Basket`); then `"$XCPROF" record --preset cpu --attach Basket --no-prompt --time-limit 30s --output "$TRACE_PATH"`. Wait on the xcodebuild job and fail if the test failed.
   - Traces go under `build/perf-traces/<label>.trace` (already covered by the existing `build/` .gitignore entry — never commit .trace bundles). Label defaults to a timestamp; the positional output-label overrides it.
   - Analyze: `"$XCPROF" analyze "$TRACE_PATH" --json` -> write `build/perf-traces/<label>.json`; also print the human markdown (`analyze` without --json) to stdout; echo the exact `xcprof compare` command to run next.
   - A `--refresh-baseline` flag copies the analyze JSON to `tools/perf-baseline/baseline.json`, pretty-printed via `jq .`.
2. Record and commit the baseline: run the script for real on this machine with `--refresh-baseline` and commit `tools/perf-baseline/baseline.json`. It must parse with `jq -e .` and contain a non-empty CPU hot-frames section with at least one frame attributed to the Basket binary (proof the attach caught the running app, not an empty trace). No separate perf-baseline README is needed; document in the main README instead.
3. Demonstrate the compare gate end-to-end: record two traces — the default workload as `baseline`, then a deliberately heavier workload (e.g. `BasketUITests/CheckOffFlowTests/testClearingWholeListShowsCelebration`) as `perturbed` — and run `"$XCPROF" compare build/perf-traces/baseline.trace build/perf-traces/perturbed.trace --fail-on-regression` (drop `--threshold-pct` to 0.1 if the default 5pp doesn't trip). Confirm exit code 3 and record the exact commands + observed exit code in the README. If it genuinely can't be demonstrated on the build machine, the README must state precisely why and show the exact commands that would be run.
4. README.md — add a 'Performance profiling (CPU)' section under the tools/testing material covering: what `tools/profile_app.sh` does and its args; the fixed default workload and why it must stay fixed; that the committed `tools/perf-baseline/baseline.json` is a reference snapshot only and `xcprof compare` needs two .trace bundles from the SAME machine; the local A/B recipe (record baseline on main -> record current on branch -> `xcprof compare A B --fail-on-regression`, exit 3 = regression); the VERBATIM honesty caveat that this is CPU-only — memory/Leaks instruments are not exportable headlessly, so this is NOT leak detection; and that CI gating is deliberately deferred (GitHub runners lack the plugin, cross-machine baselines are noise). Plain tone, no em dashes.

Do NOT add a LIMITATIONS.md row or touch any .harness/ file — the owner records the harness LIMITATIONS row separately when this lands. Do not add per-task model/effort fields anywhere.

## Done when
- `bash -n tools/profile_app.sh` passes and the script is executable.
- `tools/profile_app.sh --help` prints usage and exits 0 on a machine WITHOUT xcprof (help resolved before tool discovery); with xcprof absent a normal run exits 2 with an actionable message naming the Axiom plugin-cache path.
- Running `tools/profile_app.sh` (xcprof present) records a `.trace` under `build/perf-traces/` around the default SuggestionsFlowTests workload while the XCUITest drives the app, and emits both markdown and JSON analyze output; no `.trace` bundle is committed to git.
- `tools/perf-baseline/baseline.json` is committed, `jq -e .` parses it, and it holds a non-empty CPU hot-frames section with at least one frame attributed to the Basket binary.
- The compare dry-run is demonstrated: baseline vs a heavier workload compared with `xcprof compare ... --fail-on-regression` exits 3, and the README records the exact commands + observed exit code (or documents precisely why it is infeasible on this machine and shows the commands).
- README.md has the 'Performance profiling (CPU)' section including the verbatim CPU-only honesty caveat (memory/Leaks not exportable headlessly — not leak detection) and the explicit statement that CI gating is deferred.
- The diff touches ONLY tools/, README.md, and .gitignore — no .harness/ file, no app code, no project.yml, no Tests/UITests/, no ci.yml.
