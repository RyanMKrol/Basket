## Overview
Consolidated owner-voice guidance update for the two new diagnostics so the autonomous loop's builder and auditor actually use them. This touches .harness/custom/** so per repo policy it is a needs-human task: the human owner edits the guidance files themselves (the loop must never write under .harness/).

## Do
## Do (human)

After the two script tasks land, add a short paragraph to `.harness/custom/visual-verify-build.md` (builder prompt) and a matching one-liner to `.harness/custom/visual-verify-audit.md` (auditor prompt), in your own voice, covering:

1. When a UI test fails in a way that looks like a timeout or element-not-found, run `./tools/export_ui_screenshots.sh --failures-only` and read the crash triage output it prints; an app crash is often the real cause and the .ips summary names it.
2. `./build_run.sh` now prints a console error check after launch (and writes screenshots/console-errors.log when non-empty). A clean screenshot with persistence errors in the console is NOT a pass; read that output as part of visual verification.

No em dashes in the added copy.

## Done when
## Done when

Both guidance files mention the failure-only export plus crash triage and the build_run.sh console error check, committed by the human owner (the loop records this task blocked, never completes it itself).
