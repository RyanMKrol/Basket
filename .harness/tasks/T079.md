## Overview
The owner no longer wants the Basket Home Screen widget. Remove the entire widget feature — the BasketWidget/ app-extension target, its widget-only Swift, its embedding in the app, its tests, and the docs that describe it — while leaving the Siri 'Add to Basket' App Intent and the App Group / shared SwiftData container intact.

## Do
Delete the whole BasketWidget/ directory and the widget-only source files (Sources/Services/WidgetReload.swift, Sources/Services/BasketWidgetSnapshot.swift, Sources/Services/CheckOffItemIntent.swift, Sources/Support/BasketWidgetKind.swift). In project.yml remove the BasketWidgetExtension target, the Basket target's `dependencies: [BasketWidgetExtension embed:true]`, and the `BasketWidgetExtension: all` scheme build entry, then run `xcodegen generate`. Remove the widget-only tests (Tests/CheckOffItemIntentTests.swift, Tests/BasketWidgetSnapshotBuilderTests.swift, Tests/BasketWidgetURLTests.swift). Because WidgetReload is deleted, remove its now-dead call in Sources/Services/AddToBasketIntent.swift (line ~76) and the WidgetReload flush in Sources/BasketApp.swift's scenePhase handler, and fix Tests/AddToBasketIntentTests.swift so it no longer references WidgetReload — the Siri intent itself must keep working. Do NOT touch the App Group entitlement, AppGroup.swift, or AppSchema.makeSharedContainer (they back Siri). Update README.md and CLAUDE.md to drop all widget descriptions in the same change. ALSO remove the now-dead 'basket://add' deep link (owner confirmed): delete Sources/Support/DeepLinkEnvironment.swift; remove the `@State shouldFocusAddBar`, the `.environment(\.shouldFocusAddBar, ...)`, and the `.onOpenURL { ... }` handler from Sources/BasketApp.swift (~lines 12, 71, 88-92); remove the `@Environment(\.shouldFocusAddBar)` property and the `.onChange(of: shouldFocusAddBar)` block from Sources/Views/ShoppingListView.swift (~lines 41, 142); remove the `CFBundleURLSchemes` entry from project.yml (~line 76) so `xcodegen generate` drops it from the regenerated Sources/Info.plist; and delete UITests/DeepLinkTests.swift. The App Group + shared SwiftData container and the Siri intent are unaffected.

## Done when
BasketWidget/ and all widget-only source files are deleted; project.yml has no BasketWidgetExtension target, no widget embed dependency, and no widget scheme entry, and `xcodegen generate` regenerates cleanly; the three widget-only test files are gone; AddToBasketIntent (Siri) and its test compile and pass with no WidgetReload references; `grep -rn 'Widget' Sources/ BasketWidget/ Tests/ UITests/` returns no widget-feature references; the 'basket://add' deep link is fully gone — DeepLinkEnvironment.swift and UITests/DeepLinkTests.swift deleted, no onOpenURL/shouldFocusAddBar references in Sources/, and no basket URL scheme in project.yml or the regenerated Sources/Info.plist (`grep -rniE 'basket://|DeepLink|shouldFocusAddBar' Sources/ Tests/ UITests/ project.yml` is empty); README.md and CLAUDE.md no longer describe the widget or the deep link.
