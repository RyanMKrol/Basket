# T015 — Add SwiftLint

**Date:** 2026-07-12

## What was done

Added `.swiftlint.yml` at the repo root and wired a `Lint` step into CI.

- `.swiftlint.yml`: default rule set, `line_length` warning at 130,
  `identifier_name` `min_length` relaxed to 1 (the codebase uses short math/UI
  locals like `w`, `h`, `q`, `u`), and `trailing_comma` /
  `implicit_optional_initialization` / `shorthand_operator` disabled, plus
  `file_length`/`type_body_length` warning thresholds raised — all to let the
  existing hand-maintained code pass with zero errors without rewriting app
  code, per the task's "tune the config, don't rewrite code" instruction.
  Excludes the generated files (`Sources/Services/EmojiTable.swift`,
  `Sources/Services/SuggestionDictionary.swift`), `tools/`, `build/`, and
  `Tests/__Snapshots__/`.
- `.github/workflows/ci.yml`: added a `Lint` step to the push-triggered `test`
  job, right after checkout and before `xcodegen generate` / the build steps
  (`brew install swiftlint && swiftlint lint`). Chose non-strict
  (`swiftlint lint`, not `--strict`) so warnings (a couple of long comment
  lines in `TipJar.swift`) stay advisory and only real errors fail CI.
- `README.md`: new `## Lint` section (before `## Tests`) documenting the tool,
  the `swiftlint lint` command, what's excluded, and that errors fail CI while
  warnings are advisory.

## Verification

- `swiftlint lint` locally: 0 errors, 7 warnings (all `line_length` in
  `TipJar.swift`), exit code 0.
- `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme
  Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — full
  XCTest + XCUITest suite green (BasketTests + BasketUITests, 0 failures).
- Native logic harness (`swiftc` compile of the pure-logic `Sources` files +
  `tools/main.swift`, then run) — `ALL PASSED`.

## Remaining

Nothing outstanding for this task.
