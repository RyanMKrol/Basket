# T034 Worklog

## 2026-07-15 — Add explicit 'salad' keyword to EmojiTable

### Work completed

1. **Added "salad" -> 🥗 mapping to gen_emoji.py**: Inserted `("salad", "🥗")` into the RAW data string in tools/gen_emoji.py on line 36, in the produce/greens section after chard and before endive. This placement keeps it with other curated greens/salad vegetables as per the emoji pipeline conventions.

2. **Regenerated EmojiTable.swift**: Ran `python3 tools/gen_emoji.py Sources/Services/EmojiTable.swift` which correctly output 2110 emoji entries (up from 2109 before), with the new salad mapping appearing in the generated table.

3. **Added regression test pin**: Added `("salad", "🥗")` to the golden subset array in tools/audit_coverage.swift at line 27, after lettuce and before cucumber, ensuring the new mapping is protected against future semantic fallback regressions.

### Verification checks run

- **SwiftLint**: Ran `swiftlint lint` — 7 pre-existing line-length warnings unrelated to this change (in TipJar.swift and UITests files). No new violations introduced.

- **Coverage audit (default mode)**: Built and ran audit_coverage in default mode:
  - Corpus: 3931 unique items across 13 files
  - Coverage: 3606 curated + 325 semantic + 0 fallback = **100% (0 fall-throughs)**
  - ✓ No regression: baseline of 0 fall-throughs maintained

- **Coverage audit (correctness mode)**: Ran `-correctness` mode:
  - Golden subset: 185 expectations tested
  - Result: **✓ All 185 golden expectations match** (including new "salad" -> 🥗 pin)

- **Native harness**: Compiled and ran pure-logic harness:
  - `swiftc Sources/Services/Emoji.swift Sources/Services/EmojiTable.swift Sources/Services/SemanticEmoji.swift Sources/Services/Suggestions.swift Sources/Services/SuggestionDictionary.swift Sources/Models/Suggestion.swift Sources/Services/Formatting.swift Sources/Services/Measure.swift Sources/Services/Seasonality.swift tools/main.swift -o /tmp/basket_check && /tmp/basket_check`
  - Result: **✓ ALL PASSED** (all 150+ emoji, measure, suggestions, seasonality tests pass)

- **XCTest suite**: Ran full simulator test suite:
  - Command: `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  - Result: **✓ All tests passed** (BasketTests + BasketUITests completed successfully)

- **Build and launch**: Ran `./build_run.sh "iPhone 17 Pro"`:
  - **BUILD SUCCEEDED** with no errors
  - App installed and launched on simulator
  - Screenshot captured: `/Users/ryankrol/Development/basket-loop/screenshots/latest.png`
  - Visual verification: Pastel Dots theme intact, pixel fonts rendering correctly, app layout and styling as expected

### Regression check: "salad cream" still → 🥫

Verified: `grep '"salad cream"' Sources/Services/EmojiTable.swift` confirms `("salad cream", "🥫")` unchanged. The substring-matching phase of the emoji cascade (which checks complexKeywords before simpleKeywords) ensures "salad cream" still matches its multi-word entry before "salad" is checked, preserving the canned-goods emoji for the condiment.

### Files modified (all within scope)

- `tools/gen_emoji.py` — Added ("salad", "🥗") to RAW data
- `tools/audit_coverage.swift` — Added ("salad", "🥗") to golden array
- `Sources/Services/EmojiTable.swift` — Regenerated from gen_emoji.py (not hand-edited)

### Notes

- The task is a clean bugfix with no architectural changes or breaking behavior
- All Definition-of-Done checks complete and passing
- No dependencies or blockers
