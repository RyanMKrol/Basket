# T078 — Hardcode the chosen blur intensity + remove the comparison switch

**Done by hand** (loop not running), 2026-07-20.

Baked in the owner's T077 choice (**Light, 3 pt**) and removed all of the T076
comparison scaffolding:

- `ShoppingListView`: replaced `@State selectedBlurStyle` with a single
  `private let focusedBlurRadius: CGFloat = 3`; the `.blur(radius:)` now reads
  it. The focus-driven behaviour is unchanged (blur the list while the add bar +
  suggestions stay sharp), still animated via `.easeInOut(...).unlessUITesting`.
- Deleted the temporary segmented `Picker` from the bottom `safeAreaInset`.
- Deleted `Sources/Views/BlurStyle.swift` (the debug enum) entirely.
- Removed the throwaway capture UITest.

No `BlurStyle` / `selectedBlurStyle` references remain. Definition of Done green:
`build_run.sh` → BUILD SUCCEEDED; native harness ALL PASSED; full simulator
suite 105 XCTest + 34 XCUITest, 0 failures. Final focused state visually
verified (switch gone, no layout gap, 3 pt blur applied).
