# T079: Remove Home Screen widget + basket:// deep link

**Completed:** 2026-07-20

## What was done

Removed the entire Basket Home Screen widget feature and the now-dead
`basket://add` deep link, while keeping the Siri "Add to Basket" App Intent
and the App Group / shared SwiftData container intact.

### Removed

- `BasketWidget/` (whole app-extension target's Swift sources, entitlements, Info.plist)
- `Sources/Services/WidgetReload.swift`
- `Sources/Services/BasketWidgetSnapshot.swift`
- `Sources/Services/CheckOffItemIntent.swift`
- `Sources/Support/BasketWidgetKind.swift`
- `Tests/CheckOffItemIntentTests.swift`
- `Tests/BasketWidgetSnapshotBuilderTests.swift`
- `Tests/BasketWidgetURLTests.swift`
- `Sources/Support/DeepLinkEnvironment.swift`
- `UITests/DeepLinkTests.swift`

### Edited

- `project.yml`: dropped the `BasketWidgetExtension` target, the `Basket`
  target's `dependencies: [BasketWidgetExtension embed:true]`, the
  `BasketWidgetExtension: all` scheme build entry, and the app's
  `CFBundleURLTypes` (`basket://` scheme); ran `xcodegen generate` to
  regenerate `Basket.xcodeproj` + `Sources/Info.plist`.
- `Sources/Services/AddToBasketIntent.swift`: removed the dead
  `WidgetReload.reloadTimelines()` call after save — the Siri intent itself
  is unchanged otherwise.
- `Sources/BasketApp.swift`: removed the `WidgetReload.reloadTimelines()`
  flush in the scenePhase `.background` handler, the `shouldFocusAddBar`
  state + environment injection, and the `onOpenURL` deep-link handler.
- `Sources/Views/ShoppingListView.swift`: removed the
  `@Environment(\.shouldFocusAddBar)` property and its `.onChange` block.
- `Tests/AddToBasketIntentTests.swift`: dropped the
  `testPerformNudgesWidgetReloadAfterSave` test and the `WidgetReload` reset
  in `tearDown()`; the intent's other tests are untouched.
- `CLAUDE.md`: dropped widget-specific version-bump language (widget's
  `Info.plist` no longer needs to match the app's).

Untouched (as required): the App Group entitlement, `AppGroup.swift`,
`AppSchema.makeSharedContainer()`, and the Siri "Add to Basket" App Intent
itself.

## Checks run

- `xcodegen generate` — regenerates cleanly, no widget target/entries.
- `grep -rn 'Widget' Sources/ BasketWidget/ Tests/ UITests/` — empty.
- `grep -rniE 'basket://|DeepLink|shouldFocusAddBar' Sources/ Tests/ UITests/ project.yml` — empty.
- Native logic harness (`tools/main.swift`) — all checks passed.
- `xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=Basket-Claude'` — **TEST SUCCEEDED** (BasketTests + BasketUITests, 0 failures).
- `./build_run.sh` — **BUILD SUCCEEDED**, app launched cleanly on the simulator, no console errors.

## What remains

Nothing outstanding for this task. The repo-root `README.md`'s widget/deep-link
mentions are intentionally left for the maintainer to clean up separately, per
the task spec.
