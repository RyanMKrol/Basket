# T004 worklog

## 2026-07-12

### Build outcome
Added `os.Logger` logging to four silent `try?` persistence paths in BasketApp and KnownItems:

1. **BasketApp.swift:59** — background save on scene phase change: now logs at error level
2. **BasketApp.swift:79** — fetch count in seedIfEmpty: now logs at error level on fetch failure, preserves count=0 fallback
3. **BasketApp.swift:87** — seed save in seedIfEmpty: now logs at error level, preserves skip-save fallback
4. **KnownItems.swift:11** — upsert fetch in rememberAdd: now logs at error level, preserves treat-as-no-existing-row fallback

All changes follow the existing pattern established in TipJar.swift (subsystem = bundle id, category = "persistence").

### Verification
- ✓ Full test suite passes: `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — 23/23 UI tests pass
- ✓ Native logic harness passes: 70/70 checks pass (Emoji, Suggestions, Seasonality, Measure, etc.)
- ✓ No force-try (`try!`) introduced
- ✓ Exact fallback semantics preserved (skip save, count 0, skip seed-save, treat as no-existing-row)
- ✓ Scope respected: only modified Sources/BasketApp.swift and Sources/Services/KnownItems.swift

### Notes
- Added `import os` to both files to access Logger
- Each catch block logs the error before executing the original fallback behavior
- Logger instances use `Bundle.main.bundleIdentifier ?? "com.ryankrol.basket"` for subsystem, matching TipJar
