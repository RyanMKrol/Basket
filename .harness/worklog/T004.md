# T004 — heart.fill icon vertically off relative to title in titleView

Date: 2026-07-04

## What I did

In `Sources/Views/ShoppingListView.swift`, `titleView` (~line 162): changed the
HStack alignment from `.firstTextBaseline` to `.center`, so the heart.fill icon
centers against the title's layout bounding box instead of a baseline the
custom pixel font (Silkscreen, via `Theme.title`) reports unreliably.

Verified visually: temporarily forced `supporter` truthy (`true ||
tipJar.hasTipped`), ran `./build_run.sh`, and inspected the resulting
screenshot (3x crop of the header). With `.center` alignment the heart's
optical center sits level with the vertical midline of the rainbow "BASKET"
title — no residual manual offset needed, so no `.offset`/`.alignmentGuide`
was added. Reverted the temporary `supporter` force back to
`tipJar.hasTipped` afterward and rebuilt to confirm the app still builds
clean in its normal (non-forced) state.

Did not touch the outer `header` HStack or any other alignment in the file,
per the task's scope.

## Checks run

- `./build_run.sh` — `** BUILD SUCCEEDED **`, screenshot saved to
  `screenshots/latest.png` (both with `supporter` forced true, to confirm
  the fix visually, and again with the real `tipJar.hasTipped` flag).
- `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme
  Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — all 19
  tests passed.
- Native logic harness (`swiftc … tools/main.swift` + run) — `ALL PASSED`.

## What remains

Nothing outstanding for this task. Only checked the rainbow title variant
visually (supporter's default `titleRainbow` state); the plain-title variant
uses the same HStack so the same centering applies.
