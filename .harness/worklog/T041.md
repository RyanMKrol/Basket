# T041 worklog

## 2026-07-16

**What was done:**

Added `assertStaysGone` bounded-wait helper to `UITests/BasketUITestCase.swift` that polls for element existence over a configurable settle window (default 1.5s), failing fast the moment the element appears. Implemented using the existing `waitUntilExists` predicate wait — never sleeps.

Updated the Synchronization comment block in BasketUITestCase to explicitly mention that absence checks must go through `assertStaysGone` (not bare `XCTAssertFalse(...exists)`), extending the house rule to cover negative assertions.

Converted all 7 identified XCTAssertFalse(...exists) call sites:
- DeepLinkTests.swift:67 (testNormalLaunchDoesNotAutoFocus)
- CheckOffFlowTests.swift:106 (testClearLastItemShowsCelebration)
- CheckOffFlowTests.swift:122 (testClearAllEmptiesGotItSection)
- CheckOffFlowTests.swift:172-173 (testClearAllExpiredToastLeavesItemsCleared) — two calls
- CheckOffFlowTests.swift:199-200 (testClearToastExpiryDoesNotFlickerGotSection) — two calls
- RenameFlowTests.swift:38 (testRenameItem)
- PersistenceFlowTests.swift:67-68 (testExpiredItemsPurgedOnRelaunch) — with preserved explanatory message
- UsualsFlowTests.swift:49 (testNoUsualsWithoutHistory) — already had a 2s bounded wait, kept window

**Checks run:**

- `xcodegen generate` — succeeded
- `xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — **passed**, all tests green

**Result:** Done. All conversions complete, test suite passes.
