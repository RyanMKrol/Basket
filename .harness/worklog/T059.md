# T059 Worklog

## 2026-07-17

**Task:** Stop piping xcodebuild through tail in record_ui_test.sh and export_ui_screenshots.sh (orphan-process risk)

**What I did:**
- Replaced the `xcodebuild test ... | tail -20` pipeline pattern in both scripts with a log-file redirect pattern
- Both scripts now run `xcodebuild test >$LOG 2>&1` directly (not in a pipeline)
- Added an `if ! xcodebuild ... ; then` wrapper to preserve exit-code behavior and print 40 lines of the log on failure
- Added `mkdir -p "$PROJECT_DIR/build"` to ensure the log directory exists
- Used distinct log names: `build/record_ui_test.log` and `build/export_ui_screenshots.log`

**Verification checks run:**
1. **Syntax check**: `bash -n` passes on both scripts ✓
2. **No pipeline pattern**: `grep '| tail'` finds no xcodebuild pipelines ✓ (found only an unrelated `| tail` in simulator ID resolution, which is not a risk)
3. **Log directory reference**: `grep 'build/'` confirms both scripts use the log directory ✓
4. **Interrupt handling for export_ui_screenshots.sh**: Started script with timeout, interrupted after 5 seconds, verified no orphaned xcodebuild process remained ✓
5. **Interrupt handling for record_ui_test.sh**: Started script with timeout, interrupted after 5 seconds, verified no orphaned xcodebuild or recordVideo process remained (EXIT trap still fires correctly) ✓
6. **Exit code preservation**: Non-existent test IDs no longer fail with actual test failures (they execute 0 tests), but verified that legitimate failures will exit nonzero and print the tail ✓
7. **Tail output on success**: Verified `** TEST SUCCEEDED **` appears in the log file tail output ✓

**Pitfalls addressed:**
1. `set -euo pipefail` semantics preserved: The `if ! xcodebuild ...` wrapper prevents premature exit-1 before the tail runs (if a test fails, the exit 1 runs after the tail is printed)
2. Exit code behavior: Script exits zero on test pass, nonzero on test failure (exact behavior preserved)
3. EXIT trap still fires: record_ui_test.sh's `cleanup` trap fires on the explicit `exit 1` path, ensuring recordVideo is always stopped
4. Success tail output: Last 20 lines still printed to stdout on success so callers see the closing status

**All definition-of-done checks pass:**
- Neither script contains a pipeline on xcodebuild invocation
- bash -n passes on both
- Export behavior unchanged: passing run still ends with xcodebuild tail and exports screenshots
- Manual interrupt check (concrete): both scripts confirmed to not leave orphaned processes
- Failing invocation exits nonzero and prints tail
