# T046 Worklog

## 2026-07-17

### Completed
Added an XCTest-host detection hook and used it in `BasketApp.init` to bypass the real App Group
SwiftData store and starter-item seeding whenever the app is hosting BasketTests.

### Changes Made

**Sources/Services/TestHooks.swift:**
- Added `static let isHostedByXCTest = NSClassFromString("XCTestCase") != nil`, with a doc comment
  explaining XCTest is only loaded in-process when Basket hosts BasketTests (project.yml's
  `dependencies: [target: Basket]`), and is nil under XCUITest (separate process, no XCTest link).

**Sources/BasketApp.swift:**
- Extended the container cascade in `init()`: kept the existing `TestHooks.storeURL` /
  `TestHooks.isUITesting` order, added a new `else if TestHooks.isHostedByXCTest` branch choosing an
  in-memory `ModelConfiguration`, before the final real App Group store branch.
- Changed the seed guard to `if !TestHooks.startEmpty && !TestHooks.isHostedByXCTest` so a unit-test
  host never seeds starter items.
- Left `Self.registerFonts()` and the `TipJar` `@State` property untouched, per spec (SnapshotTests
  and TipJarTests depend on their current behaviour).

**Tests/TestHostTests.swift (new):**
- `testHostedByXCTestIsTrueUnderUnitTestHost` asserts `TestHooks.isHostedByXCTest == true`.
- Confirmed test-first: ran it before implementing the hook — it failed to compile/resolve to
  `false` against the pre-change `TestHooks` (the symbol didn't exist yet), then passed once the
  hook + branch were added.

**README.md:**
- Added a paragraph under the `Tests/` section explaining the unit-test-host bypass and pointing at
  `TestHostTests.swift`.
- Added a follow-up paragraph documenting the deliberate PARTIAL nature of the bypass: store +
  seeding are inert under a unit-test host, but `registerFonts()` and `TipJar` creation still run
  their production paths (needed by `SnapshotTests.swift` / `TipJarTests.swift`).

### Checks run
- `xcodegen generate` — regenerated the project after adding `Tests/TestHostTests.swift`.
- `./build_run.sh` — **BUILD SUCCEEDED**, ran on `Basket-Claude`, screenshot captured.
- Full suite: `./tools/loop_sim.sh >/dev/null && xcodegen generate && xcodebuild test -project
  Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=Basket-Claude'` — **TEST
  SUCCEEDED**, 130/130 tests passed (0 failures), including all 35 UITests unchanged and
  `TestHostTests.testHostedByXCTestIsTrueUnderUnitTestHost`.
- Native pure-logic harness (`tools/main.swift` against the `Sources/Services` logic files) — ALL
  PASSED.
- Empirical isolation check (Done-when #4): confirmed no other Basket `xcodebuild` process was
  running, then `xcrun simctl erase` on the dedicated `Basket-Claude` simulator, followed by
  `xcodebuild test ... -only-testing:BasketTests` (95/95 passed). Searched
  `~/Library/Developer/CoreSimulator/Devices/<udid>/data/Containers/Shared/AppGroup` for `*.store*`
  afterwards — **none found**, confirming the unit-test run never created or seeded the real App
  Group SwiftData store. Re-ran `./build_run.sh` afterwards to leave the simulator in a normal,
  verified state (BUILD SUCCEEDED).

### No changes needed
- `Sources/PrivacyInfo.xcprivacy` — no new required-reason API usage.
- `.harness/docs/LIMITATIONS.md` — out of this task's scope; the owner records that row separately.

### Remaining
Nothing outstanding for this task.
