# T013 — Port Measure, Seasonality, Formatting checks to XCTest

**Status:** Done

## What was done

Ported three service modules' tests from the native harness (`tools/main.swift`) into proper XCTest files:

- **Tests/MeasureTests.swift** — 14 test methods covering:
  - Type classification for known/unrecognised items
  - Default units and value stepping (including new g↔kg conversion)
  - Unit conversion within scale pairs (ml↔L, g↔kg) and across kinds
  - Formatting, parsing, and number display
  - **New coverage:** g↔kg conversion (line 55–57), the 1000-threshold stepping boundary (lines 37–41 with test at 999g)

- **Tests/SeasonalityTests.swift** — 9 test methods covering:
  - Time-of-day classification
  - Holiday accent emoji with full boundary testing
  - **New coverage:** Oct 23/24 boundary (Halloween start), Dec 26/27 boundary (New Year start), Jan 1/2 boundary
  - Empty-state line rotation across days using fixed dates (no live clock)

- **Tests/FormattingTests.swift** — 1 test method:
  - String capitalisation with edge cases

## Tests run

- **Full XCTest suite:** `xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — **ALL TESTS PASSED** (22 UITests + 22 BasketTests)
- **Native harness:** Recompiled and ran `tools/main.swift` — **ALL PASSED**

## Notes

- All new tests use fixed deterministic dates (no `Date.now`), following Basket conventions
- Tests follow the style and structure of existing BasketTests.swift
- XcodeGen automatically picked up the new test files after regenerating the project
- The specification's gaps (g↔kg conversion, 1000-threshold boundary, holiday boundary days) are now fully covered in the simulator suite
- `tools/main.swift` left untouched per spec (dedupe is a later decision)
