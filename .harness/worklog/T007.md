# T007: Stable tie-break in emoji keyword sort (generator)

## 2026-07-12

### Changes Made
- **tools/gen_emoji.py** (lines 305, 308): Updated both `.sorted` calls to include deterministic tie-breaking on keyword alphabetically when lengths are equal.
  - Old: `.sorted { $0.0.count > $1.0.count }`
  - New: `.sorted { $0.0.count > $1.0.count || ($0.0.count == $1.0.count && $0.0 < $1.0) }`
- **Sources/Services/EmojiTable.swift**: Regenerated from the updated generator to include the tie-break in both `complexKeywords` and `simpleKeywords` sort expressions.

### Verification
- ✓ Native harness (pure-logic test): ALL PASSED (100+ tests)
- ✓ Full simulator suite: TEST SUCCEEDED (22 UI tests, 0 failures)
- ✓ Generated file verified: Both sort expressions in EmojiTable.swift carry the alphabetical tie-break

### Result
Build clean. Both keyword sorts now deterministically resolve equal-length matches by alphabetical order, eliminating sort instability that could cause "rice" vs "cake" mappings to flip across compiler versions.
