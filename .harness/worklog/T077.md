# T077 — Pick the winning background-blur intensity (human review)

**Done by hand** (loop not running), 2026-07-20, with the owner.

The three T076 intensities were captured on the `Basket-Claude` simulator in the
focused state (add bar focused → background blur + segmented switch visible),
via a throwaway `BasketUITests` capture that cycled the switch and screenshotted
each. The owner compared them:

- **Light — 3 pt** ← **CHOSEN**
- Medium — 8 pt
- Heavy — 16 pt

**Decision: Light, `blurRadius = 3` pt.** The owner preferred the gentlest
softening — the list stays recognisable behind the focused add bar rather than
dissolving into a wash. This exact value is baked in by T078.
