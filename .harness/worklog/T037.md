# T037: Got-it row check control glyph swap

**Date:** 2026-07-16

## What was done

Swapped the CheckCircle glyph in Sources/Views/ItemRow.swift from a checkmark to an up-arrow ("arrow.up.left") to better communicate the "tap to undo" affordance for checked-off items in the Got-it section.

**Code change:** Line 229 in ItemRow.swift
- Changed `Image(systemName: "checkmark")` → `Image(systemName: "arrow.up.left")`
- All other CheckCircle behavior unchanged (green fill circle, spring animation, tap-to-untoggle wiring, accessibility labels)

**Snapshot update:** Regenerated `Tests/__Snapshots__/SnapshotTests/testItemRowCheckedInGotSection.1.png` to reflect the new arrow glyph

## Verification

✅ **Build:** `./build_run.sh` completed successfully → `** BUILD SUCCEEDED **`

✅ **Tests:** Full test suite passed
- Ran: `xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
- Result: **Executed 33 tests, with 0 failures** (all UI tests + snapshot tests passed)
- Specifically verified:
  - `SnapshotTests.testItemRowCheckedInGotSection()` — passes with new arrow reference
  - `AccessibilityAuditTests` — still passes (accessibility label unchanged)
  - `CheckOffFlowTests` — still passes (queries by accessibility ID, not glyph)

✅ **Visual verification:** 
- Screenshot shows app launching and rendering correctly with Pastel Dots theme
- Snapshot test image confirms the green circle now renders the up arrow ("↖") instead of a checkmark
- The animation and styling remain unchanged

## Notes

The snapshot test initially failed with a perceptual precision mismatch when the code was changed but the reference image still had the old checkmark. This was resolved by extracting the newly rendered snapshot from the simulator's tmp directory and replacing the reference file with it, allowing the test to pass.

No changes to accessibility labels, tap behavior, or other row interactions—this is purely a visual glyph swap.
