# T050 worklog

## 2026-07-17

### Implementation
Added Reduce Motion support to ItemRow's check-off animations per WCAG 2.3.3.

**Changes:**
1. Added `@Environment(\.accessibilityReduceMotion) private var reduceMotion` to ItemRow
2. Gated SparkleBurst at line 102: `.overlay { if isChecking && !reduceMotion { SparkleBurst() } }`
3. Updated CheckCircle to accept `reduceMotion` parameter and use `.easeInOut(duration: 0.25)` animation when Reduce Motion is on, `.spring(response: 0.42, dampingFraction: 0.6)` when off
4. Updated README.md to note that the spark burst respects Reduce Motion

All animation selections keep the `.unlessUITesting` suffix for UI test determinism.

### Verification

**Build:** `./build_run.sh` reached `** BUILD SUCCEEDED **`

**Tests:** Full suite green
- `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=Basket-Claude'` 
  - Executed 35 tests (BasketTests + UITests), 0 failures
  - SnapshotTests passed (default environment with reduceMotion=false renders identically)
  - Tap precision tests passed
  - All check-off flow tests passed

**Reduce Motion behavior verified:**
- Enabled Reduce Motion in simulator: `xcrun simctl spawn <udid> defaults write com.apple.Accessibility ReduceMotionEnabled -bool true`
- Ran CheckOffFlowTests recording and verified no sparkle particles appear during check-off (only check circle fill animation)
- Disabled Reduce Motion: `xcrun simctl spawn <udid> defaults write com.apple.Accessibility ReduceMotionEnabled -bool false`
- Ran CheckOffFlowTests recording again to confirm sparkles appear normally

**No em dashes** in any user-facing copy touched (README update used standard dash).

### Status
Ready for merge. All Definition of Done checks passed:
- ✅ Reduce Motion gates both SparkleBurst and CheckCircle animation
- ✅ Full suite green (build + tests)
- ✅ Behavior verified with Reduce Motion enabled and disabled
- ✅ No em dashes in copy
