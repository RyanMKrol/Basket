# T005 — Model initializer defaults bypass AppClock

**Date:** 2026-07-12

## Summary
Fixed two model initializer defaults that bypassed test-time clock control:
- `GroceryItem.swift:28` — changed `createdAt: Date = .now` → `createdAt: Date = AppClock.now`
- `KnownItem.swift:16` — changed `lastAddedAt: Date = .now` → `lastAddedAt: Date = AppClock.now`

Also added `import SwiftUI` to both files to bring `AppClock` into scope (defined in `Services/TestHooks.swift`).

## Verification
✓ Native harness (pure logic) — all tests passed  
✓ Full simulator suite (XCTest + XCUITest) — all 23 tests passed  
✓ Grep confirms no remaining `= .now` in `Sources/Models/`  
✓ All call sites already pass `AppClock.now` explicitly, so no production behavior changes

## Changes
- `Sources/Models/GroceryItem.swift` — added SwiftUI import, changed default
- `Sources/Models/KnownItem.swift` — added SwiftUI import, changed default
