# T028 Worklog

## 2026-07-14

### Implementation
- **URL scheme registration:** Added `CFBundleURLSchemes` with `basket` to `project.yml` and regenerated with `xcodegen generate`
- **Environment value:** Created `DeepLinkEnvironment.swift` with `shouldFocusAddBar` environment key to pass focus state from `BasketApp` to `ShoppingListView`
- **Deep link handler:** Added `.onOpenURL` modifier to `ShoppingListView` in `BasketApp` to detect `basket://add` URLs and set `shouldFocusAddBar` state
- **Focus propagation:** Added `@Environment` read of `shouldFocusAddBar` in `ShoppingListView` and `.onChange` handler to focus the add bar when the environment variable changes

### Testing
- **UI Tests:** Created `DeepLinkTests.swift` with three comprehensive tests:
  - `testColdLaunchWithDeepLink()` — verifies cold launch with deep link focuses add bar and raises keyboard
  - `testDeepLinkOnAlreadyRunningApp()` — verifies deep link refocuses add bar on already-running app without affecting list state
  - `testNormalLaunchDoesNotAutoFocus()` — verifies normal launches (no URL) don't auto-focus
- **All tests passed:** All 3 new tests passed in XCUITest suite
- **Existing tests:** All 30 existing UI tests continue to pass (zero regressions)
- **Pure-logic tests:** All native harness tests passed (85/85 checks)
- **Build:** Full suite builds cleanly, SwiftLint passes

### Visual verification
- **Cold launch screenshot:** Add bar visible with keyboard button, text field focused (green cursor visible)
- **Already-running app screenshot:** Deep link properly refocuses add bar, keyboard button visible, list state preserved
- **Normal launch screenshot:** No keyboard button visible, add bar not focused (expected)

### Documentation
- **README updated:** Added "Deep-link quick-add" feature description explaining the `basket://add` URL scheme, use case with widget quick-add button, and behavior on cold/warm launches

### Accessibility
- The add bar's existing accessibility identifiers are used (no new identifiers needed)
- The keyboard dismiss button provides visual feedback that the field is focused

## Verification checklist
- ✅ Cold launch with deep link: add bar focused, keyboard up
- ✅ Already-running app with deep link: add bar refocused, keyboard up, list preserved
- ✅ Normal launch: keyboard not auto-raised (existing tests green)
- ✅ Full test suite: 30 UI tests + pure logic tests pass
- ✅ SwiftLint: all checks pass
- ✅ Build: succeeds cleanly
- ✅ README: updated with feature description
