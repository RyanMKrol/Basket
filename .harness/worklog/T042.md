# T042: Remove nightly flake-hunt CI job

**Status:** Done

## What was done

Removed the scheduled nightly flake-hunt job from `.github/workflows/ci.yml` and updated documentation to reflect its removal:

1. **Deleted the flake-hunt job** (lines 91-128 in ci.yml):
   - Removed the entire `flake-hunt` job definition that ran tests up to 5 times on schedule
   - Removed its artifact upload step

2. **Removed the schedule trigger** (line 12-13 in ci.yml):
   - Deleted the `schedule` block with cron `"17 3 * * *"` since flake-hunt was its only consumer
   - Updated the comment above `on:` to remove reference to the schedule

3. **Updated CLAUDE.md**:
   - Removed mention of the scheduled nightly flake-hunt job from the "Definition of done" section
   - Kept the explanation of the push-triggered retry policy intact

4. **Updated README.md**:
   - Removed description of the nightly flake-hunt job from the CI section
   - Kept explanation of the push-triggered job and its retry policy

5. **Removed stray references**:
   - Deleted comment about flake-hunt in the push-triggered test job

## Verification

✓ **CI validation**: grep found no remaining `flake-hunt` references across all three files  
✓ **XCTest + XCUITest suite**: Passed with 0 failures (test gate retry policy intact)  
✓ **Native logic harness**: All 200+ checks passed  
✓ **YAML syntax**: ci.yml remains valid with `push` and `workflow_dispatch` triggers only  
✓ **Push-triggered job**: Untouched, retry policy (`-retry-tests-on-failure -test-iterations 2`) remains exactly as specified

## Scope compliance

Changes made only to files in scope:
- ✓ `.github/workflows/ci.yml`
- ✓ `CLAUDE.md`
- ✓ `README.md`

No other files touched.

Built: 2026-07-16
