# T001 — Document the harness dashboard in README

Date: 2026-07-04

## What I did

Added a "Harness dashboard" subsection to README.md's "Build & run" section
documenting that `node .harness/dashboard/server.js` starts a local read-only
viewer for the `.harness/tracking/TASKS.json` backlog (task status,
dependencies, recent worklog). Doc-only change, no Swift source touched.

## Checks run

- Native logic harness (`tools/main.swift` + `Sources/Services/*`,
  `Sources/Models/Suggestion.swift`) compiled and run: `ALL PASSED`.
- `xcodegen generate` + `xcodebuild test -project Basket.xcodeproj -scheme
  Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`: 19 tests,
  0 failures, `** TEST SUCCEEDED **`.

## What remains

Nothing — this task is mechanical and doc-only, both DoD test commands pass
unchanged, as expected.
