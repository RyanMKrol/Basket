# T001 — Replace tautological seasonality test

## 2026-07-12

### What was done

Replaced the tautological self-comparison check at lines 197–198 in `tools/main.swift` with two real assertions:

1. **Same-day stability check**: Added assertion that `Seasonality.emptyStateLine` returns the same value at two different times on the same day (08:00 and 22:00 on 2026-03-10). This verifies the property that the empty-state line is stable within a calendar day.

2. **Cross-day rotation check**: Added assertion that the empty-state line differs across at least one pair of different days (sampled 2026-03-11 and 2026-03-15 against 2026-03-10). This verifies that the line rotates across day boundaries.

The `seasonDate` helper already had an hour parameter (defaulting to 12), so no changes to that function were needed. Kept the existing `check(_:_:)` reporting style and comment conventions.

### Verification

- ✅ Native logic harness: compiled and all checks pass (`swiftc … tools/main.swift && run`)
- ✅ Full test suite: `xcodegen generate && xcodebuild test -project Basket.xcodeproj -scheme Basket -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  - XCTest: all passed
  - XCUITest: 23 tests, all passed
  - Total: 256.8s, 0 failures
- ✅ `Seasonality.swift` unchanged (test-only fix)
- ✅ No files changed outside `tools/main.swift`

### Scope

- Changed: `tools/main.swift` ✅ (within scope)
- No generated files, no `.harness/` files, no `.git/` files touched
